# Architecture — Backend

> Template. Populated incrementally as modules are actually built, only when explicitly requested (see [skills/review-architecture.md](skills/review-architecture.md), [agents/architecture-reviewer.md](agents/architecture-reviewer.md)). This is the backend half of [ARCHITECTURE.md](ARCHITECTURE.md) — see that file for the cross-cutting scope/summary and for [ARCHITECTURE-CLIENTS.md](ARCHITECTURE-CLIENTS.md) (client apps).

## Backend — Module Structure Convention

> Adopted 2026-07-30, replacing the earlier (never-built) `src/Modules/<Name>/{Domain,Application,Infrastructure,Api}` idea. See [reviews/2026-07-30-backend-project-analysis.md](reviews/2026-07-30-backend-project-analysis.md) (archival) for the analysis that prompted this.

- Every module lives as one or more **flat projects directly under `src/`** — never nested under a `src/Modules/` folder.
- **Single-project module**: for a module simple enough not to need assembly-level isolation between layers, use one project internally organized by folder (`Entities/` or `Domain/`, `Application/`, `Data/` or `Infrastructure/`, `Controllers/` or `Api/`). Name it `<Module>` by default — or `<Module>.Api` (e.g. `src/Identity.Api`) when the module is deliberately kept as a candidate for future extraction into an independent microservice: the `.Api` suffix then already previews the name that standalone service's own API project would have, minimizing rename churn if/when that extraction happens. Pick the naming style per module on purpose (default vs. extraction candidate), not by accident.
- **Split module**: for a module complex/large enough to justify assembly-level layering (team ownership, independent testability, avoiding a god-project), split it Clean-Architecture-style into `src/<Module>.Domain`, `src/<Module>.Application`, `src/<Module>.Infrastructure`, `src/<Module>.Api`.
- **Every module — single or split — also gets a `<Module>.Contracts` project.** This is the only project other modules or the composition-root host are allowed to reference from outside the module; it exposes the module's public DTOs and service interfaces (e.g. `src/Identity.Contracts`). Referencing anything else that belongs to another module (its `Domain`/`Infrastructure`/`Api` project, or its single-project internals) is a boundary violation.
- Choosing single-project vs. split is a per-module judgment call (entity count, expected growth, team size, testing isolation needs) — there is no blanket rule forcing every module into the same shape.
- **Current state**: two modules built. `Identity` (first) is a single project — deliberately named `Identity.Api` (not just `Identity`) since it's an anticipated candidate for future extraction into an independent identity service. Its internal layering is informal (folder-based, not enforced by the compiler) and predates this convention — see [docs/KNOWN_DEBT.md](docs/KNOWN_DEBT.md) for the pending cleanup. `Notifications` (second) is also a single project, `Notifications.Api` + `Notifications.Contracts`, same `.Api`-suffix reasoning — but splits its controllers by **audience** (admin vs. self-service) rather than by resource, a pattern distinct from `Identity`. See [docs/generated/backend/modules/Notifications.md](docs/generated/backend/modules/Notifications.md) for full detail.

## Backend — Layering (per module)

| Module | Structure | Domain | Application | Infrastructure | Api | Notes |
|---|---|---|---|---|---|---|
| Identity | Single project (`src/Identity.Api` — name deliberately kept, see convention above) + `src/Identity.Contracts` (seam) | `Entities/` (folder: `User`, `Role`, `RoleClaim`, `UserClaim`, `UserLogin`, `UserRole`, `UserToken`, `UserSession`) | `Application/Users/Commands` (one CQRS command) + `Services/` (`UserService` — incl. paginated `SearchAsync(SearchUserQuery, pageNumber, pageSize)`, `RoleService` — traditional service classes); `Jwt/` (`AuthenticationService`/`IAuthenticationService` — login & refresh-token orchestration, `IDateTime`-based; `UserSessionService`/`IUserSessionService` — session persistence, listing, and revocation, also `IDateTime`-based; `JwtTokenIssuer` — claims collection, delegates signing to `JwtSigningService`; `JwtSigningService` — internal class wrapping `IOptions<JwtOptions>`, `Generate`/`Validate`, owning `issuer`/`secretKey`/`roleClaimType`; `JwtHelper` — `GenerateRefreshToken()`/`ReadClaims(jwt)`) | `Data/` (`IdentityDbContext`; table/schema constants) | `Controllers/` (`UserController` — incl. `GET user/search`, `RoleController`, `TokenController`, `UserProfileController`) | CQRS command and service-class patterns coexist for what should be one approach — see [docs/KNOWN_DEBT.md](docs/KNOWN_DEBT.md) (D1). `ClaimTypeConstants.TokenId = "jti"`, not `"tid"` — `"tid"` collides with `JwtSecurityTokenHandler`'s legacy `DefaultInboundClaimTypeMap` and breaks session-id lookup. |
| Notifications | Single project (`src/Notifications.Api` — same `.Api`-suffix convention) + `src/Notifications.Contracts` (seam) | `Entities/Notification.cs` (`: AuditableEntity`) | `Services/NotificationService.cs` (`internal`, implements `INotificationService`) | `Data/NotificationDbContext.cs`; `SignalR/{SignalRHub,IHubService,HubService,CustomIdProvider,SignalRModule}.cs` | `Controllers/{NotificationController,UserNotificationController}.cs` — split by **audience**, not resource: `NotificationController` (admin, permission-gated) vs. `UserNotificationController` (self-service, permission-less, hard-scoped via `ICurrentUser.UserId`) | No CQRS/service-class inconsistency (unlike `Identity`) — one service class throughout. See [docs/KNOWN_DEBT.md](docs/KNOWN_DEBT.md) for open functional gaps and [docs/generated/backend/modules/Notifications.md](docs/generated/backend/modules/Notifications.md) for full detail. |

## Backend — Dependency Direction

> Verified via project references, not inferred from folder names. Expected direction: `Api → Application → Domain`; `Infrastructure → Application` (implements interfaces) and `→ Domain`. Modules must not reference another module's internals directly — only its `Contracts` project.

- **Verified acyclic**: `Shared` is a leaf. `Infrastructure` → `Shared`. `Persistence` → `Shared`. `Identity.Contracts` → `Shared` (not a true leaf — transitively reaches a vendor authorization package). `Identity.Api` → `Identity.Contracts`, `Infrastructure`, `Persistence`, `Notifications.Contracts` (welcome-email `IMailService` call from `UserCreatedEventHandler` — reaches only the `Contracts` seam, never `Notifications.Api`'s internals, so no boundary violation). `Notifications.Contracts` → `Shared` (true leaf). `Notifications.Api` → `Notifications.Contracts`, `Infrastructure`, `Persistence`. `StarterKit.WebApi` (composition-root host) → `Identity.Api`, `Notifications.Api`, `Infrastructure`, `Shared`. `tests/Framework.Tests` → `Shared`, `Infrastructure`, `Persistence`. `tests/Identity.Tests` → `Identity.Api`, `Shared`, with `Identity.Api.csproj`'s `<InternalsVisibleTo Include="Identity.Tests" />` (same pattern `Shared`/`Infrastructure`/`Persistence` use for `Framework.Tests`) enabling direct unit tests of `internal` JWT orchestration classes (`AuthenticationService`, `UserSessionService`, `JwtTokenIssuer`, `JwtSigningService`).
- The reverse cross-module direction doesn't exist yet: `Notifications` uses opaque `fromUserId`/`toUserId` strings, no FK or cross-module service call into `Identity`.
- Both modules' `Controllers/` call only into services/mediator/module interfaces, never directly into `Entities`/`DbContext` — `Identity.Api` via `IAuthenticationService`/`IUserSessionService`/`IUserService`/`Mediator`, `Notifications.Api` via `INotificationService`/`IHubService`. This discipline isn't compiler-enforced (single assembly per module).

## Backend — Shared Kernel / Building Blocks

> E.g. base entities, shared abstractions, common EF Core conventions used across modules — only list what's confirmed in code, with file references.

- **`src/Shared`** (leaf project, no dependencies) — base entity/DTO wrappers over vendor `Light.Domain` (`Entities/AuditableEntity.cs`, `Entities/DomainEvent.cs`), `ICurrentUser`/`IDateTime` abstractions, `Status` value object, `PageQuery`/`IPage`, `ValidationBehaviour<,>` (FluentValidation pipeline behavior for the vendor mediator), permission-authorization building blocks under `Authorization/` (`SuperUserPolicy`, `AccessControl`, `CurrentUserBase`, internal `PolicyProvider`/`AuthorizationHandler`), `Constants/` (`ClaimTypeConstants`, `CronTimeConstants`), `Utilities/ReflectionHelper`.
- **`src/Infrastructure`** (depends on `Shared`) — CORS (`Cors/`), health checks (`HealthChecks/`), Mapster config (`Mappings/MapsterSettings.cs`), module/endpoint base classes (`Modularity/AppModule.cs` + `AppModuleEndpoint.cs`), API controller base classes + `BasicAuthAttribute` (`Endpoints/`), static Serilog bootstrap logger (`AppLogging.cs` — also exposes `CloseAndFlush()`, called alongside `Log.CloseAndFlush()` from `StarterKit.WebApi/Program.cs`'s shutdown `finally` block so the bootstrap logger's async file sink is flushed too). EF Core provider wiring, the DbContext base class, and tracking/domain-event dispatch live in `src/Persistence`, not here — do not look under `Infrastructure/Database/*`.
- **`src/Persistence`** (depends on `Shared`) — EF Core provider wiring (`DbContextExtensions.cs`, `DbProvider.cs`, `DbConnectionNames.cs`), DbContext base class (`Context/BaseDbContext.cs` — `OnModelCreating` is `sealed`; derived contexts override the `protected virtual ConfigureModel(ModelBuilder)` hook instead, which runs before the Sqlite `DateTimeOffset` fix is applied, guaranteeing the fix sees all entity/owned-type configuration regardless of what the derived context configures. Note: the only real DbContext today, `IdentityDbContext`, does **not** inherit `BaseDbContext` — it inherits ASP.NET Identity's own `IdentityDbContext<...>` instead — so this contract currently only matters for future non-Identity modules), audit/soft-delete tracking (`Extensions/TrackingExtensions.cs`) and domain-event dispatch (`Extensions/DispatchDomainEventsExtensions.cs`) meant to be called from each module's own `DbContext.SaveChangesAsync`, generic paging/result helpers (`Extensions/QueryableResultExtensions.cs` — `ToPagedAsync` clamps `pageSize` to a max of 100), migration-time runtime support (`MigrationSupport/MigrationsExtensions.cs`, `MigratorCurrentUser.cs` — not to be confused with the unrelated top-level `src/Migrations/` design-time EF projects), connection-name constants (`DbConnectionNames.cs` — includes a `Catalog` constant with no corresponding module/project yet; confirmed intentional, reserved for a planned future module, not dead code).
- **`src/Identity.Contracts`** (depends on `Shared` — not a true leaf) — first real example of the per-module `<Module>.Contracts` seam: public DTOs (`UserDto`, `RoleDto`, `TokenDto`, etc.), request types, and service interfaces (`IUserService`, `IRoleService`, `IServiceClaimService` — see [docs/KNOWN_DEBT.md](docs/KNOWN_DEBT.md) (P4) for its unimplemented/unregistered status).
- **`src/Notifications.Contracts`** (leaf project, only references `Shared`) — second `Contracts` seam: `NotificationDto`, `NotificationStatus` enum, `NotificationLookup` request type, push-message contracts (`SystemMessage`, `ForceLogoutMessage`), `INotificationService`, permission catalog (`NotificationPermissions`).
- Two modules (`Identity`, `Notifications`) are built. `Identity.Api` references `Notifications.Contracts` (`IMailService`, welcome-email side effect in `UserCreatedEventHandler`) — the first real cross-module `Contracts` consumption in this repo, staying compliant (seam-only, no reach into `Notifications.Api`'s internals). `Notifications` references neither `Identity.Contracts` nor `Identity.Api`.

## Backend — Data Access

> One `DbContext` per module is the intended default. Do not assume a single repo-wide context.

| Module | DbContext | Provider | Notes |
|---|---|---|---|
| Identity | `IdentityDbContext` (`src/Identity.Api/Data/IdentityDbContext.cs`) | Configured via `Persistence.DbContextExtensions.AddConfiguredDbContext`/`GetDbProvider` (`InMemory`/`PostgreSQL`/`MSSQL`/`Sqlite` via `IConfiguration["DbProvider"]`) | Extends `Microsoft.AspNetCore.Identity.EntityFrameworkCore.IdentityDbContext<...>` directly, so it **cannot** also extend `Persistence/Context/BaseDbContext.cs` (single inheritance) — re-applies the Sqlite `DateTimeOffset` fix manually instead, a known/accepted exception. Soft-delete is passed as `enableSoftDelete: false` even though `User` implements `ISoftDelete` — see [docs/KNOWN_DEBT.md](docs/KNOWN_DEBT.md) (D2). Indexes: `User.Created`, `UserSession.UserId` (`EntityBuilderExtensions.BuildEntities`). Session validity is checked by `UserSession` primary key via the session id read from `ICurrentUser.SessionId` (claim `ClaimTypeConstants.TokenId`, `"jti"`), trusting `builder.MapControllers().RequireAuthorization()` (`src/Infrastructure/InfrastructureModule.cs`) plus the JWT Bearer scheme to have already verified the token's signature. |
| Notifications | `NotificationDbContext` (`src/Notifications.Api/Data/NotificationDbContext.cs`) | Same `Persistence.DbContextExtensions` mechanism, registered via `AddConfiguredDbContext<NotificationDbContext>(configuration, DbConnectionNames.Identity)` | Unlike `Identity`, extends `Persistence/Context/BaseDbContext.cs`. Default schema `"system"`, one `DbSet<Notification>` → table `Notifications`. `DbConnectionNames.Identity` aliases `DbConnectionNames.Default`, so this module shares the same physical database/connection string as `Identity`, separated only by schema + table — consistent with "one DbContext per module, shared physical DB." Only index: `HasIndex(x => x.ToUserId)` — no index on `Status`; see [docs/KNOWN_DEBT.md](docs/KNOWN_DEBT.md). `Notification` doesn't implement `ISoftDelete` (no soft-delete support, by design). |

## Backend — API Surface

> REST/JSON, API-only controllers. Versioning strategy per `.claude/DEVELOPMENT.md` once observed.

| Module | Route prefix | Versioning | Notes |
|---|---|---|---|
| Identity | `UserController`/`RoleController`/`TokenController`: no controller-level `[Route]` override — rely on `VersionedApiController`'s (vendor `Light.AspNetCore.Mvc.VersionedApiController`) default `[controller]`-token convention; the exact resulting template is still unverified (vendor base class not inspected). `UserController` exposes both `GET user/search` (paginated, via `Identity.Contracts/SearchUserQuery.cs` — `SearchValue`, `[StringLength(256)]`, plus page params, returning `PagedResult<UserDto>`) and `GET user` (unbounded `IEnumerable<UserDto>`); the relationship between the two is an open decision — see [docs/KNOWN_DEBT.md](docs/KNOWN_DEBT.md) (D4). `UserProfileController`: controller-level route explicitly overridden to `api/v{version:apiVersion}/user_profile` — a **deliberate** choice (confirmed via `[Route]` attribute), not the default lowercased `userprofile`, kept for readability. Same kind of intentional-naming decision as the `Identity.Api` project name (see [Module Structure Convention](#backend--module-structure-convention)) — not an inconsistency to "fix". | Unverified overall — URL-segment version token (`v{version:apiVersion}`) confirmed present on `UserProfileController`'s explicit route; default API version, header vs. URL negotiation, and version attributes on the other controllers not yet checked. | Controllers: `UserController`, `RoleController`, `TokenController`, `UserProfileController` (`src/Identity.Api/Controllers/`). `UserProfileController` scopes every action to the caller via `ICurrentUser.UserId` (no `{id}` route param): `GET` (own profile — also checks the caller's access token is still valid via `ICurrentUser.SessionId`/`IUserSessionService.IsTokenValidAsync` and that the profile is `Active`), `GET token/list` (own sessions via `IUserSessionService.GetUserTokensAsync`), `PUT token/revoke` (revoke one of the caller's own sessions via `IUserSessionService.RevokeAsync`). `TokenController` depends on `IAuthenticationService`. |
| Notifications | `NotificationController` (route base `notification`, permission-gated `notification.read`/`notification.send`): `GET notification` (paginated search), `POST notification` (send + persist), `POST notification/force_logout` (live push only, no DB write). `UserNotificationController` (route base `user_notification`, **no `[MustHavePermission]`** — any authenticated user, hard-scoped server-side to `ICurrentUser.UserId`): `GET user_notification`, `GET user_notification/{entryId}` (side effect: marks `Read`), `GET user_notification/count_unread`. Full request/response shapes in [docs/generated/backend/modules/Notifications.md § Public Contract](docs/generated/backend/modules/Notifications.md#public-contract). | Same `api/v{version}/` prefix convention as Identity, confirmed via the admin client's actual call sites. | Also maps a SignalR hub at `/signalr-hub` (`SignalREndpoint`, `[Authorize]`, `CustomIdProvider : IUserIdProvider`) — push-only, no server-callable client→server methods. |

## Backend — Architectural Risks / Debt

See [docs/KNOWN_DEBT.md](docs/KNOWN_DEBT.md) for the current, maintained list of open backend debt/pending decisions. Resolved items aren't repeated here — see git history.

---
_Last synced: 2026-08-13_
