# Client App Overview: admin

## Purpose

Internal admin console for the ModularMonolith starter template. Per `src/app/layout.tsx` metadata: "Admin Dashboard" / "Admin dashboard for the ModularMonolith starter kit." This is the **first and only client app** in this template repo (`clients/web` does not exist — verified via directory listing of `clients/`).

Since the last sync: a **Notifications** feature landed — a topbar bell with live SignalR updates (All/Unread/Archived tab filter) plus a permission-gated `/notifications` admin management page with a "Send" action. Before that: the auth/session layer was substantially hardened — the session cookie is now **AES-256-GCM encrypted** (`lib/server/token-cipher.ts`), replacing the previous plaintext-JSON state, and a full **proactive token-refresh flow** now runs inside `src/proxy.ts` on every request (previously unwired); permissions/roles are decoded directly from the access token's JWT (`lib/server/jwt.ts`) rather than sourced elsewhere. Sign-out went from unimplemented to a real `logoutAction` that redirects back through `/login?redirect=<path>`, symmetric with `proxy.ts`'s own session-expiry redirect (the query param itself was also renamed from `from` to `redirect`) — see Auth Flow below. Each nav-bearing feature (`dashboard`, `users`, `roles`) now owns its own `NavItem` metadata via a `constants/nav-item.ts`, assembled into `NAV_ITEMS` by `constants/nav-items.ts`, with `Sidebar` computing the visible menu client-side via new `lib/shared/menu.ts` (`buildVisibleMenu`) and `lib/shared/authorization.ts` helpers — see architecture.md for the deliberate barrel-bypass this required. The shared `DataTable` gained optional per-column client-side sorting (used by the Roles table's Name/Description columns). A new, purely additive `components/shared/object-viewer/` component was ported in but is not yet wired into any page. Minor layout/styling polish: `AppShell`'s `<main>` now uses `bg-sidebar` plus a centered `max-w-7xl` content wrapper, and the sidebar's right border was dropped. The dashboard page itself **still renders static mock data** — unchanged this sync (verified: `features/dashboard/api/sample-data.ts` and its own comment "this UI shell does not call src/Identity.Api yet").

Since the last sync: the app gained an internal, framework-owned select/overlay component library — `components/foundation/`, `components/select/`, `components/command/` — built directly on `@floating-ui/react` (plus `@tanstack/react-virtual` for virtualization) instead of Radix. `components/ui/select.tsx` (the Radix `Select` wrapper) is gone; its three call sites migrated to the new `EntitySelect`/`SearchSelect`. `features/notifications/components/user-select.tsx` no longer stuffs a search `<Input>` inside a Radix `SelectContent` (a documented workaround from a previous sync) — it's now a thin wrapper over `SearchSelect`, with a real clear (X) button and a 3-character minimum before the option list narrows. The Notifications page's status filter dropped its "all" pseudo-option in favor of the new `components/ui/native-select.tsx` (hand-written, not shadcn-CLI-generated) with a real, always-reselectable placeholder. `components/shared/data-table/data-table.tsx` gained an optional `mode` prop (`"paginated"` default, `"virtualized"`, `"infinite"`, the latter two rendering through a new `data-table-virtual-body.tsx`) and an `onSortChange` prop for server-driven sort — both fully backward-compatible; the three existing tables (Users, Roles, Notifications) are unchanged.

Since the last sync: that Floating-UI-based select library is gone again — `components/select/*` (all four components) and most of `components/foundation/*` (`use-floating-popover.ts`, `options-list.tsx`, `use-async-options.ts`, `types.ts`) were deleted, including `AsyncSelect`/`MultiSelect`, which never had a UI consumer. In their place, `components/ui/` gained a standard shadcn-style **Combobox**: `popover.tsx` (a new Radix `Popover` wrapper), `command.tsx` (built on a new `cmdk` dependency), and `combobox.tsx` (`Combobox<TValue>`, composing the two with `Button`). The two real consumers — `edit-user-dialog.tsx`'s status/authProvider fields and `features/notifications/components/user-select.tsx` — were migrated from `EntitySelect`/`SearchSelect` to `Combobox`; `components/command/*` (Command Palette) is untouched and still has no consumer. Building this surfaced two real bugs, both fixed: (1) a `Popover`/`Combobox` nested inside a `Dialog` couldn't be mouse-wheel-scrolled — Radix `Dialog`'s scroll lock only recognizes content that's an actual DOM descendant of its own content node as "inside," but `Popover` portals to `document.body` by default; `DialogContent` now centers via a flex wrapper instead of a CSS `transform` (a `transform` on an ancestor would have broken the nested Popover's `position: fixed` math) and exposes its own DOM node through a new `components/foundation/portal-container.ts` context, which `PopoverContent` reads to portal into the Dialog instead of `document.body` when nested — automatic for any future Combobox-in-Dialog case, not a one-off patch. (2) A sidebar nav group containing the currently-active route could never be manually collapsed — `sidebar-nav-item.tsx` OR'd the user's toggle state with "does this branch contain the active route," so an active branch stayed forced-open regardless of clicks; `hooks/use-sidebar.tsx` now tracks explicit per-href expand/collapse overrides (a `Map<string, boolean>`, `undefined` meaning "no override yet") instead of a plain expanded-`Set`, so an explicit user collapse now wins even while a child route is active.

Since this sync: `data-table-toolbar.tsx` was restructured from one combined search+buttons row into three stacked sections — an actions row, a search row, and a `Separator` inserted between rendered sections — and gained a `customSearch?: React.ReactNode` prop (a caller-supplied multi-field filter UI) plus `onCustomSearch?: () => void` (renders an explicit "Search" button; custom filters apply on click, unlike the built-in single-field `searchValue`/`onSearchChange` text search, which stays auto-debounced). The toolbar no longer builds the Export/Refresh/Columns button cluster itself — a new `components/shared/data-table/data-table-buttons.tsx` (`DataTableButtons`) does, wrapped in a new `components/ui/button-group.tsx` (`ButtonGroup`, a shadcn-style primitive that visually connects adjacent buttons). `data-table.tsx` now computes this buttons node itself and passes it to the toolbar as a `buttons` prop, or — when `customSearch` is used — renders it inside a `rounded-md border border-border` box that now always wraps the table's content (Table/Empty/Alert/virtualized body), right-aligned above the content instead of inline in the toolbar. `features/notifications/components/notifications-data-table.tsx`'s status + recipient filters moved from a standalone block above `<DataTable>` into this new `customSearch` slot and no longer auto-navigate on every change — they hold local "pending" state, applied via `router.push` only when the "Search" button is clicked. `features/users/components/users-data-table.tsx` and `features/roles/components/roles-data-table.tsx`'s row-actions dropdown trigger changed from a `MoreHorizontal` ghost-variant icon button to a bordered (`variant="outline"`) `Pencil` icon button; the dropdown itself is unchanged (still Edit + Delete).

Since this sync: auth-token injection was decoupled from the API layer. `lib/server/http.ts` no longer accepts an `accessToken` option — every request now runs a `handlers?: HttpRequestHandler[]` pipeline (each handler mutates a shared `headers` object before `fetch()`) against a `client?: ApiClientName` resolved by a new `lib/server/api-clients.ts` registry (`ApiClients.Backend`, room for more entries later). A new `lib/server/backend-api.ts` wraps `http.ts`'s `requestJson`/`requestVoid`, auto-injecting a new `lib/server/http-handlers/bearer-token-handler.ts`'s `bearerTokenHandler` (reads the ambient session via `getSession()`) plus `client: ApiClients.Backend`; nearly every `features/*/api/*.ts` file now imports from `backend-api.ts` instead of `http.ts` directly, and their exported functions no longer take an `accessToken` parameter at all. Three call sites that run before a session cookie exists still call `http.ts` directly with an explicit token via the same file's `explicitBearerTokenHandler(accessToken)` factory: `features/auth/api/login.ts` (via `login-action.ts`), `features/auth/api/refresh-token.ts`, and `features/user-profile/api/get-current-user.ts` (called from both `login-action.ts` and `proxy.ts`'s Edge middleware, which has no `next/headers` session access). Every `*-action.ts` Server Action stopped extracting/passing `session.accessToken` to its api function but kept `resolveSession()` plus the "your session has expired" early-exit for the friendly UX message. Two new client-side hooks also landed: `hooks/use-guarded-action.ts` (`useGuardedAction()`, wraps `useTransition` with toast/pending handling for imperative non-form calls — applied to `delete-user-dialog.tsx`/`delete-role-dialog.tsx`) and `hooks/use-action-success-toast.ts` (`useActionSuccessToast()`, a `useEffect` wrapper that toasts and runs a callback once a `useActionState` result's `.success` turns true — applied at 6 call sites across 5 form dialogs), replacing hand-rolled `useTransition`/`notifySuccess` duplication. Trivial, unrelated: `features/notifications/components/notifications-data-table.tsx`'s title column gained `className: "w-[80%]"`.

Since this sync: notifications' WebSocket connection now goes straight from the browser to the backend instead of through the Next.js server. `SIGNALR_HUB_URL` in `use-notifications.ts` changed from a same-origin relative path (proxied by `next.config.ts`'s now-deleted `rewrites()`) to `process.env.NEXT_PUBLIC_SIGNALR_HUB_URL!` — an absolute backend URL, the first `NEXT_PUBLIC_`-prefixed env var in this app (requires backend CORS for the admin origin, assumed already configured backend-side, not verified this sync). `connectSignalR` is now retryable: a failed `connection.start()` logs a sanitized error (a new `sanitizeSignalRErrorMessage()` strips the raw HTML negotiate-failure body SignalR's own error message embeds) and retries after 30 seconds, cleared on unmount. Separately, the session-refresh path feeding SignalR its token was tightened: `getSignalRTokenAction()` now proactively refreshes a near-expiry session before returning the access token, via a new shared `refreshSessionIfNearExpiry()` (`lib/server/refresh-session.ts`) — this closes a gap where `proxy.ts`'s own proactive-refresh middleware excludes `/api` paths from its matcher, so a session left open on one page (no navigation) could hand SignalR a stale/expired token and get a 401. `proxy.ts` and the manual super-admin `refreshSessionAction` (`features/auth/api/refresh-session-action.ts`, previously undocumented here) were both refactored onto this and a second new shared helper, `persistSessionCookie()` (`lib/server/persist-session-cookie.ts`, a Server-Action-context-only cookie writer using `next/headers`'s `cookies()` — not usable from `proxy.ts`'s middleware/Edge context) — pure dedup, no behavior change for either. Unrelated, cosmetic: the accent color picker gained a seventh preset, `black` — the one preset with no Tailwind shade scale, so it inverts across light/dark (`--color-black`/`--color-white`) instead of stepping to a lighter shade like every other preset; `accent-color-picker.tsx`'s swatch rendering gained a `swatchColor()` helper to special-case it.

Since this sync: the Dashboard feature was deleted outright and replaced by a new **Home** feature (`features/home/`), mounted at the same route (`/`). The old `features/dashboard/` (mock stat cards, a mock users table, `sample-data.ts`) is gone entirely — verified via grep, zero remaining references to `DASHBOARD_NAV_ITEM`/`DashboardPage`/`features/dashboard` anywhere. `HomePage` (`features/home/components/home-page.tsx`, an async Server Component) resolves the session, redirects to `/login` if absent, fetches the first page of the caller's own notifications via `getMyNotificationsAction()` (now newly parameterized, see below), and renders a `ProfileSummaryCard` (avatar initials, display name, `@username`, email only if present — no placeholder for absent values, consistent with this app's existing null-display convention; reuses `user-profile`'s `getInitials`/`getDisplayName`/`UserStatusBadge` rather than duplicating them, deliberately lighter than the full `/user-profile` page — no QR code, no claims table, no session-lifecycle card) above a `Card` titled "Inbox" wrapping the new `NotificationInbox`. `constants/nav-items.ts` swapped its direct-file `DASHBOARD_NAV_ITEM` import for `HOME_NAV_ITEM` (`@/features/home/constants/nav-item`), same barrel-bypass reasoning as the other nav items; `NAV_ITEMS`' first entry changed accordingly. (Found via this sync's own verification, not part of the reported change set: the assembled tree's second-level group node was also renamed at some point from "Identity"/`/identity` to "Administration"/`/administration` — `USERS_NAV_ITEM`/`ROLES_NAV_ITEM`/`NOTIFICATIONS_NAV_ITEM`'s own hrefs are untouched, `/identity/users`/`/identity/roles` still exist and work exactly as before, and `/administration` itself has no page — same 404-if-followed shape as `/settings`.)

Since this sync: Notifications gained a real two-pane inbox UI on the Home page, backed by server-side pagination and filtering rather than a client-side slice of one fetched page. Three new components: `notification-list.tsx` (the master-list pane — unread-dot indicator, title, `fromName` if present, formatted timestamp), `notification-detail.tsx` (the detail pane — an `Empty` state when nothing is selected, an "Archived" badge, the message if present, a link to `notification.url` if present), and `notification-inbox.tsx` (`NotificationInbox`, `"use client"` — the two-pane CSS-grid layout, `Tabs` for All/Unread/Archived, and a `Pagination` footer only rendered when more than one page exists), composing the first two. `getMyNotificationsAction()` (`features/notifications/api/get-my-notifications-action.ts`) changed from a hardcoded no-arg call to `(params: Omit<NotificationLookupParams, "toUserId"> = {})`, forwarded straight to the already-parameterized `getMyNotifications()` — fully backward compatible with existing no-arg call sites.

Since this sync: fixed a real bug in the topbar bell, found while building the new inbox. `notification-bell.tsx` used to fetch one unfiltered page and filter All/Unread/Archived client-side over just that batch, while its unread badge came from the separate true-total `getUnreadCountAction()` — so the "Unread"/"Archived" tabs could disagree with the badge whenever matching items existed outside the latest page. The tab filter is now server-side: `notification-bell.tsx` calls `refresh(status)` on tab change instead of filtering locally, and renders `notifications` directly rather than a locally-computed `filteredNotifications`.

Since this sync: the bell and the new inbox now share one live SignalR connection instead of each opening its own. A new `features/notifications/context/notifications-provider.tsx` (`NotificationsProvider`/`useNotificationsContext`, same throwing-custom-hook shape as the existing `SidebarProvider`) calls `useNotifications()` exactly once and provides its full return value via Context; `components/layout/app-shell.tsx` mounts it (nested inside the existing `SidebarProvider`, wrapping both `TopBar` and the main content area) — the one mount point for the whole authenticated shell. `notification-bell.tsx` and `notification-inbox.tsx` both now read `useNotificationsContext()` instead of calling `useNotifications()` directly — the raw hook is effectively an internal implementation detail of the provider now, though still exported from its own file. Supporting this: `markAsRead(id)` in `use-notifications.ts` now returns `Promise<boolean>` (was `Promise<void>`) so callers can react to failure, and `refresh` gained an optional `status?: NotificationStatus` parameter, remembered in a `statusRef` so a SignalR-pushed `SystemMessage` re-fetch preserves whichever status filter is currently active rather than always resetting to unfiltered.

Since this sync, unrelated: fixed a noisy SignalR console error on navigation. No `not-found.tsx` exists anywhere under `app/` (verified — none found), so navigating to an unmatched route renders Next.js's default 404 within the root layout only, unmounting the entire `(dashboard)/layout.tsx` subtree — this intentionally stops any live SignalR connection mid-flight, which the browser surfaces as an abnormal WebSocket closure (code 1006) that SignalR's client logs at `LogLevel.Error` by default. `use-notifications.ts`'s `HubConnectionBuilder` chain now adds `.configureLogging(LogLevel.Critical)`, silencing that specific noise while leaving the hook's own explicit `console.error` call (inside `connection.start()`'s catch block, for genuine connect failures) untouched. Logging-verbosity fix only — the underlying missing-`not-found.tsx` fact itself is unchanged and unrelated in severity; see architecture.md's Known Architectural Risks / Debt.

Since this sync: the login `Card` markup moved from the Server Component `login-page.tsx` into the Client Component `login-form.tsx`, so the whole card can show a pending-state overlay while `loginAction` is in flight — a dimmed, centered `Spinner` overlay plus `aria-busy` on the `Card` itself, and a `<fieldset disabled={pending}>` wrapping the username/password inputs so they can't be edited/submitted again mid-request. `login-page.tsx` is now a thin async wrapper only — it reads `searchParams` for `redirect` and renders `<LoginForm redirect={redirect} />` inside a centering `<div>`; it no longer imports `components/ui/card.tsx` itself. Separately, fixed a real bug in the shared `components/ui/dialog.tsx`: `DialogContent` gained a `relative` class so its close (X) button (`absolute top-2 right-2`) positions relative to the dialog box instead of jumping to the page's own top-right corner. Also fixed, unrelated: `features/users/api/create-user-action.ts` was reading the wrong `FormData` keys (`userName`/`password` instead of the form's actual field names, `new-userName`/`new-password`), so creating a user always failed client-side with "Username and password are required." even with valid input — pure bug fix, no contract/behavior change beyond that.

## Structure

- **Router**: App Router, now rooted at `src/app/` (was `app/` directly under `clients/admin/` before this sync) — verified via directory listing; no `pages/` directory exists.
- **Package manager**: pnpm — verified via `pnpm-lock.yaml`. A `pnpm-workspace.yaml` now exists too, but it only configures pnpm's build-script approval (`allowBuilds`/`ignoredBuiltDependencies` for `sharp`, `unrs-resolver`) — it does not define a multi-package workspace; `clients/admin` is still a single, independent app.
- **Data fetching approach**: server-only, hand-written per endpoint — every `features/<name>/api/*.ts` file wraps one backend call, normalized through `lib/server/call-guard.ts`. Nearly all of them get `requestJson`/`requestVoid` from the new `lib/server/backend-api.ts`, which pre-wires the bearer-token handler and `ApiClients.Backend` (see Backend Integration below); the 3 exceptions that run before a session cookie exists (`features/auth/api/{login,refresh-token}.ts`, `features/user-profile/api/get-current-user.ts`) still import `requestJson`/`requestVoid` from `lib/server/http.ts` directly, passing an explicit token via `explicitBearerTokenHandler`. Whole-list reads happen in async Server Components (`user-profile-page.tsx`, `(dashboard)/layout.tsx`, `features/users/components/users-page.tsx`, `features/roles/components/roles-page.tsx`); writes happen via a Next.js Server Action — 8 of them, all `"use server"`, same `(prevState, formData) => Promise<{error?, success?}>` shape: `features/auth/api/login-action.ts`, `features/users/api/{create,update,force-password,delete}-user-action.ts`, `features/roles/api/{create,update,delete}-role-action.ts`. Five more Server Actions serve a third purpose — an on-demand *read* triggered from a Client Component when a list row's own data isn't complete enough, or when a picklist itself is no longer preloaded by the page (see [architecture.md](./architecture.md) for why): `features/users/api/get-user-detail-action.ts` and `features/roles/api/get-role-detail-action.ts` fetch a single record's full detail on dialog open; new this sync, `features/roles/api/get-all-roles-action.ts` and `features/roles/api/get-permissions-action.ts` (both a plain `() => Promise<{data, error?}>`) let `edit-user-dialog.tsx`/`edit-role-dialog.tsx` fetch their own role/permission picklists on open, now that `users-page.tsx`/`roles-page.tsx` no longer preload them; and `features/users/api/search-users-action.ts` (`(params: SearchUsersParams) => Promise<{data: Paged<UserDto> | null, error?}>`) backs the new on-demand recipient search in `features/notifications/components/user-select.tsx`. No client-side data-fetching library (no React Query/SWR in `package.json`) for HTTP requests — this still holds. `features/notifications` adds a new category of client-side networking, though: a persistent WebSocket (`@microsoft/signalr`) opened directly from the browser (`use-notifications.ts`), not a fetch-based library. `getAllUsers`'s previous eager-preload pattern is gone from Notifications entirely — see below. **Changed this sync**: the former mock-data Dashboard exception is gone — `/` (now `HomePage`, `features/home/`) is a real async Server Component calling `resolveSession()` and `getMyNotificationsAction()` like every other page; there is no longer any page in this app that skips the backend.
- **State management**: local component state + React Context, no global state library:
  - `hooks/use-sidebar.tsx` (`SidebarProvider`/`useSidebar`) — sidebar hidden/expanded/mobile-open state, persisted to `localStorage`.
  - `components/theme/accent-color-provider.tsx` (`AccentColorProvider`/`useAccentColor`) — accent color selection, persisted to `localStorage`, applied via `data-accent` on `<html>`. Moved here from a former `hooks/use-accent-color.tsx`.
  - `components/theme/theme-provider.tsx` — thin wrapper around `next-themes`' `ThemeProvider`. Moved here from a former top-level `providers/` folder.
  - `features/auth/components/login-form.tsx` — `useActionState` bound to the `loginAction` server action. **Changed this sync**: now also owns the full `Card` markup (moved out of `login-page.tsx`), and derives a `pending` boolean from `useActionState` to render a dimmed, centered `Spinner` overlay (`aria-busy` on the `Card`) plus a `<fieldset disabled={pending}>` around the username/password inputs while the login action is in flight.
  - `features/users/components/users-data-table.tsx` — drives search/pagination through URL `searchParams` via `router.push`/`router.refresh()` wrapped in `useTransition`; owns open/selected-row state for all three dialogs (create/edit/delete) plus a per-dialog remount-key counter used to force-remount each on every open.
  - `features/users/components/{create,edit}-user-dialog.tsx` — `useActionState` bound to their respective Server Actions, plus a parallel controlled `useState<FormValues>` for the form fields (deliberate — see [architecture.md](./architecture.md) for why). `edit-user-dialog.tsx` additionally holds its own `roles: RoleDto[]` state and, on open, runs `Promise.all([getUserDetailAction(user.id), getAllRolesAction()])` in a `useEffect` — fetching both the full user record (the list endpoint's DTO doesn't carry `roles`/`claims`) and the role picklist itself, now that `users-page.tsx` no longer preloads and passes it down.
  - `features/users/components/delete-user-dialog.tsx` (new) — now via `hooks/use-guarded-action.ts`'s `useGuardedAction()` (`[pending, run]`, wraps `useTransition` with toast/pending handling) wrapping a direct (non-form) call to `deleteUserAction`, previously hand-rolled `useTransition`.
  - `features/roles/components/roles-data-table.tsx` (new) — same shape as `users-data-table.tsx`, but drives search via local `useState` + client-side `.filter()` instead of URL params, since `RoleController` has no search/pagination endpoint.
  - `features/roles/components/edit-role-dialog.tsx` (new) — same "fetch full detail on open" pattern as `edit-user-dialog.tsx`, via `getRoleDetailAction`; likewise now holds its own `permissions: PermissionDefinition[]` state and, on open, runs `Promise.all([getRoleDetailAction(role.id), getPermissionsAction()])` since `roles-page.tsx` no longer preloads the permission catalog.
  - `features/notifications/components/user-select.tsx` (rewritten) — no longer a thin wrapper; owns its own `open`/`query`/`options`/`loading`/`selectedLabel` state. A debounced (300ms) `useEffect` calls `searchUsersAction` for page 1 of matches once the trimmed query reaches the minimum search length, replacing the previous preloaded-list + client-side-filter approach.
  - `components/shared/data-table/data-table-toolbar.tsx` — local debounced search-text state (400ms) before calling the `onSearchChange` prop (unchanged for the built-in text search); no longer owns any button-cluster state — Export/Refresh/Columns moved to the new `data-table-buttons.tsx`, driven by `data-table.tsx`'s own `hiddenColumnIds` state. `data-table-pagination.tsx` — local page-jump input state, committed on blur/Enter.
  - `features/notifications/components/notifications-data-table.tsx` (new this sync) — local "pending" status/recipient filter state (`pendingStatus`/`pendingToUserId`), applied to the URL only when the `customSearch` slot's "Search" button is clicked; re-synced from props when the URL changes externally, mirroring `data-table-toolbar.tsx`'s own search-value-sync pattern.
  - `features/notifications/hooks/use-notifications.ts` — owns `notifications`/`unreadCount`/`loading` state; opens a `HubConnection` (`@microsoft/signalr`) in a `useEffect` and refetches on every `SystemMessage` push rather than merging the pushed payload in place. **Changed this sync**: now instantiated exactly once, inside the new `features/notifications/context/notifications-provider.tsx`'s `NotificationsProvider` (see below) — effectively an internal implementation detail of the provider now, though still exported from its own file. `markAsRead(id)` now returns `Promise<boolean>` (was `Promise<void>`); `refresh` gained an optional `status?: NotificationStatus` parameter, remembered in a `statusRef` so a SignalR-pushed re-fetch preserves whichever tab is currently active.
  - `features/notifications/context/notifications-provider.tsx` (new this sync) — `NotificationsProvider`/`useNotificationsContext`, another instance of the context-provider-per-concern pattern (`SidebarProvider`/`AccentColorProvider`): calls `useNotifications()` exactly once and shares its full return value (`notifications`/`unreadCount`/`loading`/`markAsRead`/`refresh`) via Context. Mounted once in `components/layout/app-shell.tsx`, so `notification-bell.tsx` and `notification-inbox.tsx` read the same live state/connection instead of each opening its own.
  - `features/notifications/components/notification-inbox.tsx` (new this sync) — local `useState` for `notifications`/`totalPages`/`pageNumber`/`filter`/`selectedId`; on filter or page change, calls `getMyNotificationsAction({ pageNumber, status })` inside a `useTransition` (dims the panel via `opacity-60` while pending). Reads `unreadCount`/`markAsRead` from `useNotificationsContext()` rather than owning them locally.
  - `hooks/use-guarded-action.ts` (new) — `useGuardedAction()` returns `[pending, run]`; `run(action, successMessage?, onSuccess?)` wraps `useTransition`, toasting success/error and invoking `onSuccess`. Used by `features/users/components/delete-user-dialog.tsx` and `features/roles/components/delete-role-dialog.tsx`, replacing each dialog's own hand-rolled `useTransition` + manual `notifyError`/`notifySuccess` branching.
  - `hooks/use-action-success-toast.ts` (new) — `useActionSuccessToast(state, successMessage, onSuccess?)`, a `useEffect` that toasts and runs `onSuccess` once a bound `useActionState` result's `.success` turns true. Used at 6 call sites across 5 files: `features/users/components/create-user-dialog.tsx`, `features/roles/components/create-role-dialog.tsx`, `features/notifications/components/send-notification-dialog.tsx`, `features/roles/components/edit-role-dialog.tsx`, `features/users/components/edit-user-dialog.tsx` (2 call sites — the update-user form and the password-reset form) — replacing a `useEffect` + `notifySuccess(...)` pattern previously duplicated in every form dialog.
- **Styling**: Tailwind CSS v4, CSS-first configuration — `src/app/globals.css` uses `@import "tailwindcss"` plus an inline `@theme inline { ... }` block. No `tailwind.config.ts`/`.js` file. `postcss.config.mjs` wires in `@tailwindcss/postcss`. Unchanged since the last sync.

## Key Routes/Areas

| Route/Area | Path | Responsibility | Notes |
|---|---|---|---|
| Home | `src/app/(dashboard)/page.tsx` (route `/`) | Re-exports `HomePage` from `@/features/home` | New this sync (replaces the deleted Dashboard/mock-data page). Async Server Component: resolves the session (redirects to `/login` if absent), renders a `ProfileSummaryCard` plus an "Inbox" card wrapping `NotificationInbox`, whose initial page is fetched server-side via `getMyNotificationsAction()` |
| Profile | `src/app/(dashboard)/user-profile/page.tsx` (route `/user-profile`) | Re-exports `UserProfilePage` from `@/features/user-profile` | Account details, QR code of the user ID, roles/claims, all fetched live |
| Login | `src/app/login/page.tsx` (route `/login`) | Re-exports `LoginPage` from `@/features/auth` | Outside the `(dashboard)` group, no `AppShell`/session resolution |
| Dashboard-group layout | `src/app/(dashboard)/layout.tsx` | Calls `resolveSession()`, then renders `AppShell` with `permissions`/`userName`/`user` from the resolved session (or the empty/`null` equivalents) | Wraps `/`, `/user-profile`, `/identity/users`, `/identity/roles`; unauthenticated users never actually reach it in practice because `proxy.ts` redirects first. New this sync: sibling `(dashboard)/error.tsx` (rendered inside `AppShell`) and `(dashboard)/loading.tsx` (a centered `Spinner`) provide this group's error/loading boundaries, cascading to every nested route |
| Root layout | `src/app/layout.tsx` | Loads `Inter` font, wraps app in `ThemeProvider` → `AccentColorProvider` → `TooltipProvider`, mounts `<AppToaster />` (`components/toast`) as a sibling of `TooltipProvider` | `<html suppressHydrationWarning>` for `next-themes` |
| Users | `src/app/(dashboard)/identity/users/page.tsx` (route `/identity/users`) | Re-exports `UsersPage` from `@/features/users` | Full CRUD: list, create, edit, delete, force-password-reset |
| Roles | `src/app/(dashboard)/identity/roles/page.tsx` (route `/identity/roles`) | Re-exports `RolesPage` from `@/features/roles` | New this sync — this route previously had no `page.tsx` and 404'd despite the nav entry existing. Full CRUD: list (client-filtered, no server search), create (name/description only — the backend's `POST /role` accepts no claims), edit (name/description + a permissions checklist), delete |
| Notifications | `src/app/(dashboard)/notifications/page.tsx` (route `/notifications`) | Re-exports `NotificationsPage` from `@/features/notifications` | New this sync. Gated on `notification.read`; a "Send" action gated on `notification.send`. Status + recipient filter dropdowns |

Every `page.tsx` under `src/app/` is now a one-line re-export from a feature's public barrel (`export { X as default } from "@/features/<name>";`) — routing files contain no logic.

`src/constants/nav-items.ts` still declares nav entries for `/identity` (children `/identity/users`, `/identity/roles`) and `/settings`. `/identity/users` and `/identity/roles` both now have real pages (see above); `/settings` still has **no corresponding `page.tsx`** (verified via directory listing) and still 404s if followed.

## Backend Integration

Real, but partial. `lib/server/http.ts` calls `API_BASE_URL` (required server-only env var, see `.env.example`, resolved per-client via `lib/server/api-clients.ts`'s `ApiClients` registry — only `Backend` exists today), prefixing every path with `api/v1/`. Authorization is now attached via a request-handler pipeline rather than a passed-in token: `lib/server/backend-api.ts` (the entry point for nearly every `features/*/api/*.ts` call) auto-runs `lib/server/http-handlers/bearer-token-handler.ts`'s `bearerTokenHandler`, which reads the ambient session (`getSession()`) and sets `Authorization: Bearer <accessToken>` if present; the 3 pre-session-cookie call sites (`login.ts`, `refresh-token.ts`, `get-current-user.ts`) instead pass the same file's `explicitBearerTokenHandler(accessToken)` directly to `http.ts`. Non-2xx responses now surface a real message where available: `send()`'s new `extractErrorMessage` helper reads the response body and prefers, in order, the app's own `ApiResponse.message`, an ASP.NET `ValidationProblemDetails.errors` map, or a `ProblemDetails.title`, falling back to the old generic "request failed with status N" message only if none parse — this is shared-kernel and applies to every `features/*/api/*.ts` call, not just `users`.

Endpoints currently wired, by feature:
- **`auth`**: `token/token/get` (login, POST — path has a doubled `token/token` segment because `TokenController`'s own route plus its action route both contribute `token`, per an explicit comment in `login.ts`), `token/token/refresh` (`refreshToken()`, POST) — **now wired up**: called from `lib/server/refresh-session.ts`'s `refreshSession()`, itself invoked proactively by `src/proxy.ts` when the access token is close to expiring (see Auth Flow).
- **`user-profile`**: `user_profile` (`getCurrentUser`, GET), `user_profile/token/list` (`listSessions`, GET) and `user_profile/token/revoke` (`revokeSession`, PUT) — both exist and are exported from the feature barrel but have **no UI consumer** yet.
- **`users`**: `user/search` (GET, `searchUsers`), `user` (GET all, `getAllUsers`; PUT-by-id, `updateUser`; DELETE-by-id, `deleteUser`), plus get-by-id, create, force-password — the `Identity.Api` `UserController` surface this app wraps, one function per file. **Changed this sync**: `get-user-by-username.ts` was deleted — it was an unused frontend wrapper (zero remaining references anywhere in `src/`); the backend's `GetByUsername` endpoint itself is untouched, this app simply no longer calls it. Every write path has a real UI consumer at `/identity/users` (search, create, edit, delete, force-password-reset); `get-user-by-id` has a second caller via `get-user-detail-action.ts`, the edit dialog's on-open detail fetch (see [architecture.md](./architecture.md)'s "fetch full detail on open" pattern). `user/search` gained a new consumer this sync, `search-users-action.ts` — used by `features/notifications/components/user-select.tsx`'s on-demand recipient/sender search, not `users-page.tsx`'s own list search. `getAllUsers` is now fully unconsumed again — the Notifications page used to call it unpaginated to populate its recipient picker; that preload is gone this sync (see below).
- **`roles`**: `role` (GET all, `getAllRoles`; POST, `createRole`; PUT, `updateRole`; DELETE-by-id, `deleteRole`), plus get-by-id — the full `Identity.Api` `RoleController` surface. Full CRUD at `/identity/roles`; `get-role-by-id` is used via `get-role-detail-action.ts` for the edit dialog's on-open fetch. `getAllRoles` gained a second consumer this sync via the new `get-all-roles-action.ts` — `roles-page.tsx` still calls it directly for the list itself, but `edit-user-dialog.tsx` now also calls it (through the new action) to self-fetch the role picklist on open, since `users-page.tsx` no longer preloads and passes one down.
- **`permissions`**: `permissions` (GET, `getPermissions`) — the full catalog of definable permissions (`PermissionDefinition { name, displayName, parent }`), backing the Roles edit dialog's permissions checklist. **Changed this sync**: no longer fetched server-side in `roles-page.tsx` — `edit-role-dialog.tsx` now self-fetches it on open via the new `get-permissions-action.ts`, alongside its existing role-detail fetch.
- **`home`**: none directly — `HomePage` (`features/home/`) itself makes no HTTP call beyond `resolveSession()` (cookie-only, no fetch) and `getMyNotificationsAction()`, which is the same `user_notification` endpoint the `notifications` feature already wires (see below). Deleted this sync: the former `dashboard` feature, which called nothing and read `STAT_SUMMARIES`/`SAMPLE_USERS` from a hardcoded `features/dashboard/api/sample-data.ts`.
- **`notifications`**: `notification` (GET, `getNotifications` — admin search/list; POST, `sendNotification` — admin send), both permission-gated backend-side (backend also exposes `notification/force_logout`, with **no frontend caller**); `user_notification` (GET, `getMyNotifications` — self-scoped), `user_notification/{id}` (GET, `markNotificationRead` — marks-as-read as a side effect), `user_notification/count_unread` (GET, `getUnreadCount`). Plus a direct WebSocket connection straight to the backend's `/signalr-hub` (`NEXT_PUBLIC_SIGNALR_HUB_URL`, an absolute URL — no longer proxied same-origin through `next.config.ts`, whose `rewrites()` was deleted entirely; requires backend CORS for the admin origin), authenticated via a short-lived access token handed to the browser by `getSignalRTokenAction()` — the one deliberate place the access token leaves the httpOnly cookie boundary described in Auth Flow below; `getSignalRTokenAction()` also proactively refreshes a near-expiry session first (`refreshSessionIfNearExpiry()`), so a long-open tab can't hand SignalR a stale token. The connection retries after a failed handshake (30s, via `use-notifications.ts`'s retryable `connectSignalR`), logging a sanitized error rather than the SignalR client's raw HTML-embedding error message; `.configureLogging(LogLevel.Critical)` was added to the `HubConnectionBuilder` chain this sync, silencing SignalR's own noisy `console.error` for the expected abnormal-closure (code 1006) that happens whenever `(dashboard)/layout.tsx` unmounts (e.g. hitting an unmatched route — there is still no `not-found.tsx` anywhere under `app/`). `notifications-page.tsx` no longer calls `getAllUsers` to populate the recipient filter/picker or resolve the "To" column's display name — the recipient picker (`user-select.tsx`) searches `user/search` on demand instead, and the "To" column renders `notification.toUserId` directly. **New this sync**: `getMyNotifications`/`getMyNotificationsAction` now also power `HomePage`'s inbox (`NotificationInbox`), which pages/filters server-side via `getMyNotificationsAction({ pageNumber, status })` rather than the previous unfiltered-first-page-only shape; the topbar bell's own tab filter (`notification-bell.tsx`) was fixed this sync to go through the same server-side `refresh(status)` path instead of filtering a fetched batch client-side, so the bell's Unread/Archived tabs can no longer disagree with its own unread-count badge. Both the bell and the inbox now share one live connection/`unreadCount` via a new `features/notifications/context/notifications-provider.tsx` (`NotificationsProvider`), mounted once in `AppShell`.

Each API function returns a normalized `Result`/`ApiResponse`-shaped envelope via `guardCall`/`guardResponseCall`/`guardRawCall` in `lib/server/call-guard.ts`, matching the backend's own `Result<T>` envelope (`types/api.ts`).

Both `/identity/users` and `/identity/roles` gate on permissions via `lib/server/authorization.ts`'s `hasPermission(session, userName, permission)`. Permission-string constants now live per-feature rather than in one shared file: `features/users/constants/permissions.ts` (`USERS_PERMISSIONS`) and `features/roles/constants/permissions.ts` (`ROLES_PERMISSIONS`) — the former `constants/permissions.ts` is gone. The string values were also corrected this sync to match the backend's actual format (`Identity.Contracts/Authorization/IdentityPermissions.cs`): lowercase, dotted, `identity`-prefixed (e.g. `identity.users.view`, `identity.roles.manage`), not the previous `Users.View`-style strings, which never matched anything the backend issues. Missing the `*.View` permission renders an inline "Access denied" `Empty` block instead of redirecting; missing `*.Create`/`*.Update`/`*.Delete`/`*.Manage` just hides the corresponding action.

## Auth Flow

Cookie-based session, encrypted at rest, with proactive token refresh:

1. `/login` → `LoginForm` (`features/auth/components/login-form.tsx`) submits to `loginAction`, a `"use server"` Server Action (`features/auth/api/login-action.ts`). When the page was reached via `/login?redirect=<path>`, `LoginPage`/`LoginForm` carry that path through as a hidden `redirect` form field (see step 4). **Changed this sync**: the `Card` UI itself (header, form, submit button) now lives entirely in `LoginForm` (a Client Component), not `LoginPage` (the async Server Component) — `LoginForm` derives `pending` from `useActionState` and renders a dimmed, centered `Spinner` overlay plus a `<fieldset disabled={pending}>` around the inputs while the action is in flight; `LoginPage` is now just a thin wrapper that resolves `searchParams` and centers `<LoginForm redirect={redirect} />` on the page.
2. `loginAction` calls `login()` (POST `token/token/get`). On success it also calls `getCurrentUser()` for display/profile data (a failure here doesn't block login — `profile` defaults to `null`). **Permissions and roles are decoded directly from the access token's JWT** (`lib/server/jwt.ts`'s `extractPermissions`/`extractRoles`, reading the `permission`/`role` claim types) — never from the profile API. `SessionData.claims` is the union of every claim in the JWT (`extractAllClaims`) and the profile API's raw claims, deduped via `dedupeClaims()` (`lib/server/build-session-claims.ts`'s `buildSessionClaims()`).
3. The resulting `SessionData` (access/refresh token, absolute access-token expiry, a hard 7-day absolute `sessionExpiresAt` — `SESSION_TTL_MS`, not extended by refresh — claims, permissions, roles, profile) is JSON-serialized and **encrypted** (`lib/server/token-cipher.ts`'s `encrypt()`, AES-256-GCM, keyed by `TOKEN_ENCRYPTION_KEY`) before being written to the `admin_session` cookie (`httpOnly`, `sameSite: "lax"`, `path: "/"`, `maxAge` derived from `sessionExpiresAt`). The former "plaintext for now" state is gone: `TOKEN_ENCRYPTION_KEY` is a hard requirement now (`lib/server/config.ts`'s `getTokenEncryptionKey()` throws if unset) and is read on every encrypt/decrypt via `token-cipher.ts`.
4. On success, `loginAction` reads the submitted `redirect` field and redirects there if it's a safe same-site relative path (`startsWith("/")` and not `startsWith("//")` — guards against an open redirect), falling back to `/`.
5. `src/proxy.ts` (the Next.js 16 "proxy" convention file, successor to `middleware.ts`) runs on every non-static/non-API request. It decrypts and validates the cookie (`lib/server/parse-session.ts`'s `parseSessionCookie()` → `token-cipher.ts`'s `decrypt()`); a missing/expired/malformed session (past `sessionExpiresAt`) redirects to `/login?redirect=<path>` and clears the cookie, and visiting `/login` with a still-valid session redirects to `/`. It calls the shared `refreshSessionIfNearExpiry(session)` (`lib/server/refresh-session.ts` — **changed this sync**: extracted out of `proxy.ts`'s own inline `REFRESH_LEAD_MS` check into a reusable helper, also now used by `getSignalRTokenAction()`, see Backend Integration), which proactively rotates the token via `refreshSession()` (calling the backend's `token/token/refresh` via `features/auth/api/refresh-token.ts` and re-decoding permissions/roles from the newly-issued token) once within `REFRESH_LEAD_MS` (5 minutes) of expiry — a not-due check and a failed refresh both return `null` and neither is treated as a dead session (the code's own comment: multi-tab refresh races can look identical to a real failure, and the backend's own calls still enforce revocation). On a hard navigation (full page load, detected via the `Sec-Fetch-Dest: document` header — Next.js's own client-side route transitions don't send it) or right after a refresh, it also refetches display/claims data via `getCurrentUser()` and rewrites the re-encrypted cookie on both the outgoing request and the response. **This is the token-refresh flow the previous doc version reported as unwired — it is now fully implemented, inside `proxy.ts` rather than a separate scheduled job or client-side call.**
6. `logoutAction` (`features/auth/api/logout-action.ts`, `"use server"`) deletes the session cookie and redirects to `/login?redirect=<path>` (same open-redirect guard as login) or plain `/login`. `components/layout/user-menu.tsx`'s "Log out" item calls it with the current path (`usePathname()` + `useSearchParams()`). **This closes a previously-documented gap** ("Sign-out has no implementation") — session-expiry (via `proxy.ts`) and explicit logout now both funnel through the same `/login?redirect=<path>` → post-login-redirect pattern.
7. `lib/server/session.ts`'s `getSession()` reads and decrypts the cookie (no fetch). `features/user-profile/api/resolve-session.ts`'s `resolveSession()` is now a thin passthrough to `getSession()` — it no longer makes a live `getCurrentUser()` call itself, since `proxy.ts` already keeps the cookie's profile/claims fresh on every hard navigation and after each refresh. Used by the dashboard-group layout, the profile page, and both `UsersPage`/`RolesPage`.

Note: `token-cipher.ts` uses Node's built-in `crypto` module (`createCipheriv`/`createDecipheriv`) directly, and `proxy.ts` has no explicit `export const runtime` pin — see [architecture.md](./architecture.md#known-architectural-risks--debt) for the resulting flag around which runtime this is verified to require.

## External Dependencies

From `package.json` `dependencies` (changes since last sync noted):

- **`next` 16.2.12, `react`/`react-dom` 19.2.4** — framework/runtime, unchanged.
- **`radix-ui` ^1.6.7** — unified Radix primitives, base for `components/ui/*`.
- **`class-variance-authority` ^0.7.1** — variant class composition.
- **`clsx` ^2.1.1 + `tailwind-merge` ^3.6.0** — combined in `lib/shared/utils.ts`'s `cn()` (moved from a former root `lib/utils.ts`).
- **`lucide-react` ^1.28.0** — icon set.
- **`next-themes` ^0.4.6** — theme switching; also now drives `components/toast/toaster.tsx`'s light/dark toast theme.
- **`qrcode` ^1.5.4** — generates the data-URI QR code of the user ID on `/user-profile` (`features/user-profile/components/user-profile-page.tsx`).
- **`server-only` ^0.0.1** (new this sync) — compile-time marker forcing a build error if any `lib/server/*` module using it is ever imported into a Client Component; added to all 15 files under `lib/server/`.
- **`sonner` ^2.0.7** — toast notifications, wrapped by `components/toast/` (`AppToaster`, `notifySuccess`/`notifyError`); called from the Users and Roles create/edit/delete dialogs' success paths.
- **`shadcn` ^4.16.0** — CLI + runtime stylesheet import (`app/globals.css` imports `shadcn/tailwind.css`).
- **`tw-animate-css` ^1.4.0** — animation utility classes.
- **`@microsoft/signalr` ^10.0.0** — real-time push client; used only by `features/notifications/hooks/use-notifications.ts`.
- **`@floating-ui/react` ^0.27.20** — positioning + interaction primitives (`useFloating`, `useListNavigation`, `useTypeahead`, `useDismiss`, `useRole`, `FloatingPortal`, `FloatingFocusManager`). **Narrower this sync**: no longer used by any select/combobox component (the new `Combobox` is Radix-based instead) — only the surviving `components/foundation/{use-listbox,use-virtual-list,floating-overlay}.ts(x)` and the Command Palette (`components/command/*`) still depend on it.
- **`@tanstack/react-virtual` ^3.14.9** — headless list virtualization; used by `components/foundation/use-virtual-list.ts` for the DataTable's `"virtualized"`/`"infinite"` modes and the Command Palette's result list past 50 items. **No longer** backs an option list (`options-list.tsx` was removed this sync) — the new `Combobox` does not virtualize its option list, a deliberate scope decision (see [architecture.md](./architecture.md#known-architectural-risks--debt)), not a regression.
- **`cmdk` ^1.1.1** (new this sync) — the filterable-list primitive behind `components/ui/command.tsx`, itself the base of the new `components/ui/combobox.tsx`.

From `devDependencies`: unchanged this sync (`@tailwindcss/postcss`/`tailwindcss`, `eslint`/`eslint-config-next`, `prettier`/`prettier-plugin-tailwindcss` — still no `.prettierrc*`/`format` script, `typescript`, `@types/node`, `@types/react`, `@types/react-dom`, `@types/qrcode`).

## Relationship to Other Client Apps

`clients/admin` is still the only subfolder under `clients/` (verified). It remains fully independent — the new `pnpm-workspace.yaml` only configures pnpm build-script approval, it is not a multi-app monorepo workspace root. Nothing is shared with a sibling app because none exists yet.

## Notes

<!-- manual: content below this line is human-authored and must be preserved verbatim during sync -->

---
_Generated: 2026-08-10 (resynced — SignalR now connects directly from the browser to the backend via `NEXT_PUBLIC_SIGNALR_HUB_URL` instead of a `next.config.ts`-proxied same-origin path (`rewrites()` deleted); `use-notifications.ts`'s `connectSignalR` retries on a failed handshake (30s) and logs via a new `sanitizeSignalRErrorMessage()`; `getSignalRTokenAction()` and `proxy.ts` now share a new `refreshSessionIfNearExpiry()` helper (`lib/server/refresh-session.ts`) and a new `persistSessionCookie()` helper (`lib/server/persist-session-cookie.ts`), also adopted by the previously-undocumented `features/auth/api/refresh-session-action.ts`; accent color picker gained a `black` preset with inverted light/dark tokens; further resynced same day — the Dashboard feature (mock stat cards/users table) was deleted outright and replaced by a new `features/home/` mounted at `/`, rendering a `ProfileSummaryCard` plus a two-pane `NotificationInbox` (new `notification-list.tsx`/`notification-detail.tsx`/`notification-inbox.tsx`); `getMyNotificationsAction()` gained real params (`Omit<NotificationLookupParams, "toUserId">`); fixed a real bug where the topbar bell's Unread/Archived tabs could disagree with its own unread-count badge, by making the tab filter server-side; added a shared `features/notifications/context/notifications-provider.tsx` (`NotificationsProvider`) so the bell and the new inbox share one live SignalR connection/`unreadCount` instead of two; silenced a cosmetic SignalR console error on navigation via `.configureLogging(LogLevel.Critical)`; noted (found via verification, not a reported change) that the nav tree's "Identity" group node was renamed to "Administration"; further resynced same day — the unused `get-user-by-username.ts` API wrapper was deleted (zero remaining references) and its barrel re-export removed, so `users` in Backend Integration no longer lists `get-by-username`; the new `server-only` npm package was added and `import "server-only";` prepended to all 15 `lib/server/*` files, verified via a real `pnpm build`/`pnpm start` not to break `proxy.ts`'s Edge/middleware runtime; new `src/app/error.tsx`, `src/app/(dashboard)/error.tsx`, and `src/app/(dashboard)/loading.tsx` boundaries added; 7 Server Actions (`create/update/delete-role-action`, `create/update/delete-user-action`, `send-notification-action`) gained a self-invalidating `revalidatePath` call on success, following the existing `refresh-session-action.ts` precedent) — further resynced 2026-08-11 — login `Card` markup moved from `login-page.tsx` (Server Component) into `login-form.tsx` (Client Component) to show a pending overlay (`Spinner` + `aria-busy`) and disable inputs (`<fieldset disabled={pending}>`) while `loginAction` runs; fixed `components/ui/dialog.tsx`'s `DialogContent` close-button positioning bug (`relative` added so `absolute top-2 right-2` resolves against the dialog, not the page); fixed `create-user-action.ts` reading the wrong `FormData` keys, which had made user creation always fail client-side — scope: client app "admin" — see .claude/CLAUDE.md for update rules._
