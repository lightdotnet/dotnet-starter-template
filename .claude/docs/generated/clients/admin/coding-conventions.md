# Coding Conventions: admin

## Build & Tooling

- **Next.js**: 16.2.12, App Router (`src/app/` — moved under `src/` this cycle, was `app/` at the package root; no `pages/` directory exists) — verified in `package.json` and directory listing.
- **Bundler for `next dev`**: `unknown` — `package.json`'s `"dev": "next dev"` has no explicit `--turbopack` flag and `next.config.ts` sets no bundler option. Unchanged/unverified from last sync.
- **Package manager**: pnpm — `pnpm-lock.yaml` present, no `package-lock.json`/`yarn.lock`. A `pnpm-workspace.yaml` now exists but only configures pnpm's build-script approval (`allowBuilds`/`ignoredBuiltDependencies` for `sharp`, `unrs-resolver`), not a multi-package workspace.
- **`tsconfig.json` strictness**: `"strict": true`, target `ES2017`, module resolution `"bundler"`, path alias `"@/*"` → `"./src/*"` — **changed** from `"./*"` to match the new `src/` root. `components.json`'s finer-grained aliases were updated to match: `utils` → `@/lib/shared/utils` (was `@/lib/utils`), `css` → `"src/app/globals.css"` (was `"app/globals.css"`); `components`/`ui`/`lib`/`hooks` aliases (`@/components`, `@/components/ui`, `@/lib`, `@/hooks`) are unchanged strings, now resolving under `src/` via the updated `tsconfig.json` mapping.
- **Styling toolchain**: Tailwind CSS v4 via `@tailwindcss/postcss`, CSS-first config (`src/app/globals.css`) — unchanged.

## Style

- **Linting**: ESLint 9 flat config (`eslint.config.mjs`), `eslint-config-next`'s `core-web-vitals` + `typescript` rule sets. A scoped override disables `react-hooks/refs` (the React Compiler ESLint rule) for `src/components/{foundation,select,command}/**` — a known false-positive category, since `@floating-ui/react`'s `context`/`refs` objects are read throughout render by design, not the `.current`-during-render hazard the rule targets. No other file in the repo is affected. **Stale as of this sync**: `components/select/*` was deleted, so that glob segment now matches nothing (harmless no-op, not yet cleaned up — see [architecture.md](./architecture.md#known-architectural-risks--debt)); the new `components/ui/{popover,command,combobox}.tsx` are not covered by this override and haven't needed it.
- **Formatting**: `prettier` + `prettier-plugin-tailwindcss` are devDependencies; still no `.prettierrc*` config file and no `format` script — unchanged, still `unknown` whether formatting is enforced anywhere.
- **Component file naming**: kebab-case filenames, one primary export per file matching a PascalCase component/function name — unchanged, now also applied consistently across `features/*/api/*.ts` (one function per file, named after the endpoint it wraps, e.g. `get-current-user.ts` exports `getCurrentUser`).
- **`"use client"` directive**: applied to files using hooks/state/browser APIs — `hooks/*`, `components/theme/*` (except none currently omit it), `components/layout/*` except `brand.tsx`, `components/shared/search-box.tsx`, `features/dashboard/components/users-table.tsx`, `features/auth/components/login-form.tsx`, `components/shared/data-table/{data-table,data-table-toolbar,data-table-buttons,data-table-pagination,data-table-virtual-body}.tsx` (`data-table-buttons.tsx` new this sync), `components/toast/toaster.tsx` (needs `next-themes`' `useTheme()`), `features/users/components/{users-data-table,create-user-dialog,edit-user-dialog,delete-user-dialog}.tsx`, `features/roles/components/{roles-data-table,create-role-dialog,edit-role-dialog,delete-role-dialog}.tsx`, `features/notifications/hooks/use-notifications.ts`, `features/notifications/components/{notification-bell,notifications-data-table,send-notification-dialog,user-select}.tsx`, every file in `components/foundation/*` and `components/command/*`, and `components/ui/native-select.tsx`, `components/ui/{popover,command,combobox}.tsx`, and `components/foundation/portal-container.ts` (`components/select/*`, previously in this list, is deleted). `components/ui/button-group.tsx` (new this sync) is the one exception among the newly-added `components/ui/*` files — it has no hooks and omits the directive, consistent with the "Server Components without hooks" carve-out below. Server Components without hooks (`components/layout/brand.tsx`, `features/dashboard/components/stat-card.tsx`, `features/auth/components/login-page.tsx`, `features/user-profile/components/user-profile-page.tsx`, `features/users/components/users-page.tsx`, `features/roles/components/roles-page.tsx`, `features/notifications/components/notifications-page.tsx`, `components/ui/button-group.tsx`, all `app/**/page.tsx` and `app/**/layout.tsx`, all `features/*/api/*.ts` files) omit it and run server-side — the async data-fetching components (`user-profile-page.tsx`, `login-page.tsx`, `(dashboard)/layout.tsx`, `users-page.tsx`, `roles-page.tsx`, `notifications-page.tsx`) are `async function`/Server Components, not client-fetched.
- **`"use server"` directive**: 19 modules across seven shapes. 9 use the mutation shape `(prevState, formData) => Promise<{error?: string; success?: boolean}>`, consumed via `useActionState`: `features/auth/api/login-action.ts`, `features/users/api/{create,update,force-password,delete}-user-action.ts`, `features/roles/api/{create,update,delete}-role-action.ts`, `features/notifications/api/send-notification-action.ts`. 2 use the id-arg on-demand-read shape — a plain `(id: string) => Promise<{data, error?}>` called directly (not bound to a `<form>`): `features/users/api/get-user-detail-action.ts`, `features/roles/api/get-role-detail-action.ts`. `features/auth/api/logout-action.ts` (`(redirectTo?: string) => Promise<void>`) is the outlier that returns nothing — it deletes the session cookie and calls `redirect()` itself. Four more shapes from `features/notifications/api/`: a **no-arg read** shape, `get-my-notifications-action.ts` (`() => Promise<{data, error?}>`); a **fire-and-forget-safe** shape, `get-unread-count-action.ts` (`() => Promise<number>`, swallowing any failure to `0` rather than surfacing an error); an **id-arg boolean** shape, `mark-notification-read-action.ts` (`(id) => Promise<boolean>`); and a **token-handoff** shape, `get-signalr-token-action.ts` (`() => Promise<{accessToken} | null>`), which exists specifically to let a Client Component obtain the access token for the SignalR handshake. **New this sync**, three more modules: `features/roles/api/get-all-roles-action.ts` and `features/roles/api/get-permissions-action.ts` both fit the existing **no-arg read** shape (`() => Promise<{data, error?}>`, same bucket as `get-my-notifications-action.ts`) — they exist so `edit-user-dialog.tsx`/`edit-role-dialog.tsx` can self-fetch their picklist on open. `features/users/api/search-users-action.ts` takes a params object rather than an id or no args — `(params: SearchUsersParams) => Promise<{data: Paged<UserDto> | null, error?}>` — a **params-object read** shape, distinct from the existing id-arg/no-arg read shapes; used by `features/notifications/components/user-select.tsx`'s on-demand search.
- **shadcn-generated primitives** (`components/ui/*`, 28 files, up from 24 two syncs ago — `popover.tsx`/`command.tsx`/`combobox.tsx` added then, `button-group.tsx` new this sync): unchanged `data-slot="<name>"` + `React.ComponentProps<...>` + `cva()` convention, with two exceptions. `popover.tsx`/`command.tsx` follow the convention despite being hand-written rather than CLI-generated (same reasoning as `native-select.tsx`); `combobox.tsx` is a plain composition of the three and doesn't itself wrap a Radix/`cmdk` primitive, so it has no `data-slot`. `button-group.tsx` (new this sync) uses `data-slot="button-group"`/`React.ComponentProps<"div">` but has **no `cva()`** — it's structural (border/rounding utilities toggled by `orientation`), not a variant-driven primitive; provenance (CLI vs hand-written) is `unknown`.
- **UI kit visual conventions** (mechanical, no behavior change): focus/invalid rings use `ring-2` (was `ring-3`) across `button.tsx`, `input.tsx`, `textarea.tsx`, `checkbox.tsx`, `switch.tsx`, `radio-group.tsx`, plus the hand-rolled search inputs (`components/shared/search-box.tsx`, `components/shared/data-table/data-table-toolbar.tsx`); text-entry-style inputs use `rounded-md` (was `rounded-lg` — containers/buttons/popovers keep `rounded-lg`); Radix list-style rows the user clicks (`dropdown-menu.tsx`'s item variants, `sidebar-nav-item.tsx`'s expandable-group button) use `cursor-pointer` (was `cursor-default`/native default). `select.tsx` (Radix) and its Floating-UI successor `components/select/*` are both gone as of this sync. The current single-select, `components/ui/combobox.tsx`, is a composition of `popover.tsx` (Radix) and `command.tsx` (`cmdk`) and so inherits the shared `ring-2`/`rounded-lg` popover tokens directly, rather than reproducing them by hand the way the deleted `EntitySelect`/`SearchSelect` had to; `native-select.tsx` remains the one hand-rolled exception (no Radix/`cmdk` underneath a real `<select>`), still following the same tokens (`ring-2`, `rounded-md`) by hand.

## Structural Conventions

- **Feature folder convention**: `features/<name>/` colocates `api/` (one file per backend endpoint, wrapping `lib/server/http.ts` + `lib/server/call-guard.ts`), `components/`, optionally `types/` (only when a type has exactly one consumer, e.g. `features/roles/types/{role,permission-definition}.ts`), optionally `hooks/` (`features/notifications/hooks/use-notifications.ts` — the first feature-owned hook), optionally `constants/` (permission strings, and one `nav-item.ts` per nav-bearing feature, see below), and a mandatory `index.ts` barrel that re-exports the feature's public surface. **Cross-feature imports must go through the barrel** — verified in the places it's exercised: `features/auth/api/login-action.ts` and `features/users/{components/users-page.tsx,api/create-user-action.ts}` all import from `@/features/user-profile`'s barrel, not `@/features/user-profile/api/get-current-user` directly. **Changed this sync**: the previous `features/users/components/users-page.tsx` → `@/features/roles` (barrel — `getAllRoles`) and `features/notifications/components/notifications-page.tsx` → `@/features/users` (barrel — `getAllUsers`) examples are both gone — neither page fetches that data anymore. **Five narrow, deliberate exceptions exist**: `constants/nav-items.ts` imports each feature's `nav-item.ts` by direct file path rather than via the barrel; `components/layout/user-menu.tsx` imports `logoutAction` directly from `@/features/auth/api/logout-action` rather than the `@/features/auth` barrel (which doesn't currently export it); `components/layout/topbar.tsx` imports `NotificationBell` directly from `@/features/notifications/components/notification-bell` rather than the `@/features/notifications` barrel, since that barrel also re-exports `NotificationsPage` (an async Server Component reading cookies via `next/headers`); and, new this sync, `features/users/components/edit-user-dialog.tsx` imports `getAllRolesAction` directly from `@/features/roles/api/get-all-roles-action` rather than `@/features/roles`'s barrel, and `features/notifications/components/user-select.tsx` imports `searchUsersAction` directly from `@/features/users/api/search-users-action` rather than `@/features/users`'s barrel — both because the target barrel also re-exports an async Server Component (`RolesPage`/`UsersPage`) that reads cookies via `next/headers`. All five are driven by the same reason: the target feature's barrel also re-exports server-only code, and importing the full barrel from client-side code would drag that chain into the client bundle — a reasoned exception to the convention, not a violation, scoped narrowly to plain-data/single-component/single-function imports. See [architecture.md](./architecture.md#key-design-patterns) for the full reasoning.
- **Permission-string constants live per-feature, not in a shared file**: `features/users/constants/permissions.ts` (`USERS_PERMISSIONS`), `features/roles/constants/permissions.ts` (`ROLES_PERMISSIONS`), and `features/notifications/constants/permissions.ts` (`NOTIFICATIONS_PERMISSIONS` — `notification.read`/`notification.send`) each export plain string-literal constants kept in sync by hand with the backend's own permission-string constants, values lowercase and dotted (e.g. `identity.users.view`, `notification.send`) to match what the backend actually issues. **Nav metadata follows the same per-feature-ownership move**: `features/{dashboard,users,roles,notifications}/constants/nav-item.ts` each own their own `NavItem` (referencing the feature's own permission constant where relevant), and the top-level `constants/nav-items.ts` is purely an assembly file — it composes those into `NAV_ITEMS`, only declaring the "Identity" group and "Settings" leaf directly since neither is owned by a single feature.
- **`app/` is routing-only**: every `page.tsx`/`layout.tsx` under `src/app/` either re-exports a feature component as `default` or composes shared layout chrome (`AppShell`) plus a feature's session-resolution call — no business logic lives in `app/`.
- **API-per-endpoint-file convention**: each `features/*/api/*.ts` file wraps exactly one backend HTTP call via `requestJson`/`requestVoid` (`lib/server/http.ts`), returning a normalized result via one of `guardCall`/`guardResponseCall`/`guardRawCall` (`lib/server/call-guard.ts`) depending on whether the backend endpoint returns a `Result<T>` envelope, a bare `ApiResponse`, or a raw value/array.
- **`lib/server/` vs `lib/shared/` split**: `lib/server/*` is server-only (env access, fetch wrapper, cookie helpers/encryption, JWT decoding, token refresh) and must never be imported from a Client Component; `lib/shared/*` is safe for both server and client and is used by 2+ features or layout chrome. This sync moved permission-checking into this split explicitly: `lib/shared/authorization.ts` (new — plain `permissions: string[]`/`userName` parameters, no `SessionData` dependency, so it's usable from the client-side `Sidebar`) holds the actual logic, and `lib/server/authorization.ts` is now a thin wrapper preserving the original `(session, userName, permission)` call signature for existing server callers (`UsersPage`, `RolesPage`). `lib/shared/menu.ts` (new) is the other client-safe addition — a pure `buildVisibleMenu()` with no `lib/server/*` dependency. `lib/server/session-cookie.ts` still avoids `next/headers` (unlike `lib/server/session.ts`) so `proxy.ts` can import the cookie-name constant and refresh-timing values (`SESSION_TTL_MS`, `REFRESH_LEAD_MS`) without it; note that `proxy.ts` does now pull in Node's `crypto` transitively (via `lib/server/token-cipher.ts`), so whether it still qualifies as edge-runtime-safe is an open question — see [architecture.md](./architecture.md#known-architectural-risks--debt).
- **Client component boundary**: state/effects/browser API usage pushed into small dedicated hooks/providers (`hooks/use-sidebar.tsx`, `hooks/use-scrolled.ts`, `components/theme/{accent-color-provider,use-has-mounted}`) — unchanged pattern, some files relocated.
- **Presentational-only shared components take no feature dependency**: `components/shared/data-table/` is fully generic/controlled via props — it imports only `components/ui/*` and `lib/shared/utils`, never a `features/*` module. Same for `components/toast/`, which wraps `sonner` behind `notifySuccess`/`notifyError` rather than exposing the library directly, and for `components/shared/object-viewer/` (additive — built only on `components/ui/table`/`components/ui/input`, no page/dialog consumes it yet). `components/foundation/*` and `components/command/*` follow the same rule — they import only `@floating-ui/react`/`@tanstack/react-virtual`/`lucide-react`/each other, never `features/*`. (`components/select/*`, the third member of this group previously, is deleted this sync.) `components/ui/combobox.tsx` (new this sync) follows it too — only `components/ui/*`, `lucide-react`, `lib/shared/utils`.
- **Design-token-driven styling**: unchanged — semantic Tailwind utilities backed by CSS custom properties in `globals.css`'s `@theme inline` block.
- **Error handling**: still no error boundaries, no `error.tsx`/`not-found.tsx` under `app/`. Every mutation Server Action (`loginAction` and the 7 `{create,update,delete,...}-*-action.ts` files) surfaces failures via a typed `{error?: string}` state returned to `useActionState`, rather than throwing; the two detail-fetch actions (`get-{user,role}-detail-action.ts`) use the analogous `{data, error?}` shape. `logoutAction` is the outlier — it has no failure path to surface (deletes a cookie, redirects) and returns `void`. `call-guard.ts` normalizes thrown errors (network failure, non-2xx status, non-JSON body) into the same `Result`/`ApiResponse` shape the backend itself returns, so UI code only ever handles one shape. `lib/server/http.ts`'s `send()` also extracts a real message from non-2xx bodies (`extractErrorMessage()` — envelope `message` → `ValidationProblemDetails.errors` → `ProblemDetails.title` → generic fallback) before that error reaches `call-guard.ts`, so the normalized result usually carries a specific, backend-authored message rather than a generic one. `UsersPage`/`RolesPage` both pass a `DataTableErrorState`-shaped `error` prop into `DataTable` instead of short-circuiting to a standalone `Alert` — the table (with safe fallback data) always renders, even on a failed fetch; `DataTable` also prioritizes an in-flight reload (`isLoading`) over a stale `error` from a previous failed load, rather than the stale error masking the new loading state.

## Testing Conventions

- **Test framework(s)**: none installed — unchanged. No Jest/Vitest/Playwright/Testing Library, no `*.test.*`/`*.spec.*` files, no `test` script.
- **Naming convention / mocking library**: not applicable — no test suite exists yet, now more notable given real auth/session/API logic plus a create-user flow has landed untested.

## Deviations From Norms Elsewhere in the Repo

- Not fully evaluable — `clients/admin` is still the only client app (no sibling under `clients/` to compare against). No deviation from a documented cross-repo convention was identified; `.claude/CLAUDE.md`'s stated client-app defaults (Next.js App Router, TypeScript, React) all still hold.
- Worth noting for any future sibling app: the `src/`-rooted layout, the feature-folder + barrel convention, and the `lib/server`/`lib/shared` split introduced this cycle are conventions established here first — treat them as a candidate baseline, not yet a cross-repo norm.

## Notes

<!-- manual: content below this line is human-authored and must be preserved verbatim during sync -->

---
_Generated: 2026-08-04 (resynced — barrel-only cross-feature import exceptions three -> five (`edit-user-dialog.tsx` -> `get-all-roles-action`, `user-select.tsx` -> `search-users-action`); removed the now-false `getAllRoles`/`getAllUsers` barrel-import examples; `"use server"` module count 16 -> 19, documenting `get-all-roles-action.ts`/`get-permissions-action.ts` (no-arg read shape) and `search-users-action.ts` (new params-object read shape)) — scope: client app "admin" — see .claude/CLAUDE.md for update rules._
