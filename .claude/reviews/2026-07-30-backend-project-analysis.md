# Backend Project Analysis & Refactor Proposals — 2026-07-30

**Scope**: all backend projects under `src/` on branch `feat-dev/identity-module`, including `Identity.Api` / `Identity.Contracts`. `src/Migrations/**` generally excluded from analysis per request (exceptions noted inline where a change elsewhere forced a mechanical edit there).

This file now tracks **pending items only**. Resolved/implemented items (module-structure convention, N1–N6 naming, P2/P3 Schemas removal, Sqlite/PostgreSQL migration fix, the `IdentityDbContext`/framework name-collision bug, D3's `TokenService`/`JwtTokenManager` rename+split into `AuthenticationService`/`JwtTokenIssuer`/`UserSessionService` which also resolved P7/P8/P9/P11, and P14 — `GetUserTokensAsync`/`RevokeAsync` now wired into `UserProfileController`'s `token/list`/`token/revoke` endpoints) have been removed — see `ARCHITECTURE.md` § Backend — Module Structure Convention and git history for that work. Post-D3, the user further renamed `GenerateTokenByAsync`→`GenerateTokenAsync` and swapped the ad-hoc `TimeNow` seam for `TimeProvider` — see new findings P15/P16 below (the `TimeProvider` swap introduced a DI registration gap and a duplicate clock abstraction).

**2026-08-01 re-review**: six-agent pass (architecture/code-quality/EF Core/security/performance/API design) against the current working tree, covering the `TimeProvider` follow-through plus a new uncommitted feature — paginated user search (`Identity.Contracts/SearchUserQuery.cs`, `IUserService.SearchAsync`/`UserService.SearchAsync`, `UserController` `search` endpoint) — and the accompanying `AuthenticationService`/`UserSessionService` simplification. Found one **security regression** (P17 — introduced by the simplification, not present before) plus a priority escalation on existing P1 (P18), new pagination/search hardening items (P19–P23, P27), minor consistency nits (P24–P26), and a new open decision (D4). D1/D2/P1/P4–P16 unchanged; not re-litigated below except where noted.

## Current project graph (verified, acyclic)

```
Shared (leaf)
  ← Infrastructure
  ← Persistence
Identity.Contracts (leaf)
  ← Identity.Api  (also → Infrastructure, Persistence)
       ← StarterKit.WebApi (host, also → Infrastructure, Shared)
```

---

## Decisions needed

### D1. CQRS vs. service classes inside Identity — pick one pattern
`Identity.Api` currently has both: `UserService`/`RoleService` (traditional service classes, used directly by controllers) and a single CQRS command (`Application/Users/Commands/CreateUser.cs`) that just wraps `IUserService.CreateAsync` to publish `UserCreatedEvent`. Every other operation (roles, tokens, other user operations) bypasses the command pattern entirely. This reads as an unfinished migration rather than a deliberate layering. Recommend either:
- Drop the CQRS wrapper — publish `UserCreatedEvent` directly from `UserService.CreateAsync`; or
- Commit to CQRS for all Identity writes and demote services to internal/read-only helpers.

*(User has said this will be implemented separately.)*

### D2. Soft-delete is wired up but never actually happens — audit-trail loss, not a login-bypass risk
`AuthenticationService.GetTokenAsync`/`RefreshTokenAsync` both short-circuit on `user is null` *before* calling `CheckInvalidUser` (`AuthenticationService.cs:24,74`), so a hard-deleted user is already rejected via the null check — `CheckInvalidUser`'s `user.Deleted != null` branch is dead code, but that's incidental, not a security hole.

The actual cost of disabling soft-delete (`IdentityDbContext` calls `AuditEntries(..., enableSoftDelete: false)`):
- `User.Delete()` (`Entities/User.cs:55-65`) anonymizes the row first (nulls `UserName`/`Email`/`PhoneNumber`/names/`PasswordHash`, locks `Status`) — a soft-delete-and-anonymize pattern. But `UserService.DeleteAsync` (`Services/UserService.cs:170-186`) then immediately hard-deletes that same just-anonymized row — the anonymization work is wasted.
- Once the row is gone, anything that stamped `CreatedBy`/`LastModifiedBy`/a `UserId` FK with this user's Id (e.g. `UserSessions.UserId`, audit fields on any future module's entities) becomes an orphaned reference — an audit log showing "created by `<UserId>`" can no longer resolve who that was.
- `User.Deleted`/`DeletedBy` end up permanently `null` — dead schema; a future admin/audit feature filtering `WHERE Deleted IS NOT NULL` would always get zero rows, silently.

Decide: flip `enableSoftDelete: true` in `IdentityDbContext.SaveChanges[Async]` (keeps the anonymized tombstone; `AuthenticationService`'s check becomes the real enforcement point) — or, if hard delete is genuinely intended, remove `ISoftDelete`/the `Deleted`/`DeletedBy` columns/the dead check.

This is a **one-line change**, not new logic: `TrackingExtensions.AuditEntries` (`Persistence/Extensions/TrackingExtensions.cs:19-31`) already does the "who/when" stamping automatically when `enableSoftDelete: true` is passed, and `IdentityDbContext` already passes the right `currentUser.UserId`/`clock.AuditTime` arguments today — just the boolean flag needs flipping (or the cleanup path taken instead).

### D4. `GET user` (unbounded list) vs. `POST user/search` (paginated) now coexist — decide the relationship (new, 2026-08-01)

`UserController` exposes two different "list users" mechanisms for what's functionally the same read use case: the pre-existing `GET user` (`UserController.cs:25-29`, returns a plain unpaginated `IEnumerable<UserDto>`) and the new `POST user/search` (returns `PagedResult<UserDto>`, see P19–P23). Not a breaking change today — both are additive — but it's organic drift rather than a decision: is `GET user` an intentional "admin/all" escape hatch that should stay, or should it be deprecated now that a paginated search exists? Left as-is, a second module will have no precedent to follow for "how do we expose a list endpoint here."

---

## Correctness / performance

| # | Finding | File(s) | Recommendation |
|---|---|---|---|
| P4 | `IServiceClaimService` has no registered implementation — commented out in DI | `Identity.Api/DependencyInjection.cs`, `Identity.Contracts/Services/IServiceClaimService.cs` | Either implement and register it, or remove the unused contract until it's needed. *(User: later.)* |
| P5 | `IdentityClaimQueryExtensions.CheckUserHasClaimAsync` is dead code (never called) | `Identity.Api/Services/IdentityClaimQueryExtensions.cs` | Wire it into a permission check or remove it. *(User: later.)* |
| P6 | `DispatchDomainEvents` convention is never called from `IdentityDbContext.SaveChanges[Async]`; `UserCreatedEvent` is instead published manually via MediatR from the command handler, bypassing it | `Persistence/Extensions/DispatchDomainEventsExtensions.cs`, `Identity.Api/Data/IdentityDbContext.cs`, `Application/Users/Commands/CreateUser.cs` | Related to D1 — once a pattern is picked, make domain-event dispatch consistent across Identity. |
| P10 | `JwtTokenIssuer.GetUserClaimsAsync` (moved from the old `JwtTokenManager`) does an N+1 role-claim lookup (`FindByNameAsync` + `GetClaimsAsync` per role, in a loop) on every login/refresh | `Identity.Api/Jwt/JwtTokenIssuer.cs` | Flatten to one query for role IDs plus one batched claims query. |
| P12 | Byte-for-byte duplicated expiration-calculation block between `GetTokenAsync` and `RefreshTokenAsync` | `Identity.Api/Jwt/AuthenticationService.cs:44-45,79-80` | Extract into a small private helper (e.g. `ComputeExpirations()`). |
| P15 | **`TimeProvider` is injected into both `AuthenticationService` and `UserSessionService` constructors but is never registered in DI anywhere in the solution** (verified via repo-wide grep for `AddSingleton`/`TimeProvider`) — ASP.NET Core does not auto-provide `TimeProvider`; without an explicit `services.AddSingleton(TimeProvider.System)`, DI activation of either class throws `InvalidOperationException` at runtime (e.g. on the first `token/get` request). Confirmed by `dotnet build` succeeding (compile-time only) with no test exercising the DI graph. | `Identity.Api/Jwt/AuthenticationService.cs:16`, `Identity.Api/Jwt/UserSessionService.cs:11` | Register `TimeProvider.System` in DI (e.g. in `JwtServiceCollectionExtensions.AddJwtAuthentication`), **or** replace `TimeProvider` with the already-registered, already-tested `IDateTime`/`DateTimeService` (see P16) to avoid introducing a second registration to maintain. |
| P16 | `TimeProvider` duplicates the existing `IDateTime`/`DateTimeService` clock abstraction (`Shared/IDateTime.cs`, registered `AddSingleton<IDateTime, DateTimeService>` in `Infrastructure/InfrastructureModule.cs:13`, has its own test `DateTimeServiceTests`, and is already used by `IdentityDbContext` for audit timestamps) — two "current time" seams now exist in the same module, which is exactly the kind of mixing that caused the Sqlite `DateTimeOffset` ordering bug (`1cf07e2`). | `Identity.Api/Jwt/AuthenticationService.cs:16,48,86`, `Identity.Api/Jwt/UserSessionService.cs:11,48,79,109`, `Shared/IDateTime.cs`, `Infrastructure/InfrastructureModule.cs:13` | Pick one clock abstraction for the module (recommend reusing `IDateTime` since it's already registered/tested) rather than introducing `TimeProvider` alongside it — unless a deliberate repo-wide move to `TimeProvider` is intended, in which case `IDateTime` should be retired too, not left dangling. |
| **P17** | **Security regression — refresh-token expiry check truncated to midnight.** `RefreshTokenAsync`'s session lookup compares `x.RefreshTokenExpiresAt >= now.Date` instead of `>= now`. `.Date` discards time-of-day, so the check effectively becomes "expires on or after *today's date*" — a refresh token that already expired earlier today (e.g. at 08:00) still passes `08:00 >= 00:00` and is accepted for the rest of the day, extending its usable life by up to ~24h past configured expiry. Inconsistent with sibling checks in the same file (`GetUserTokensAsync:85-86`, `IsTokenValidAsync:115`), which both compare against full-precision `now` — confirms this is a regression from the `UserSessionService` simplification, not intentional "valid through end of day" policy. Attacker scenario: a leaked refresh token that expired earlier the same day can still be exchanged for a fresh access token via `token/refresh`. | `Identity.Api/Jwt/UserSessionService.cs:55` | Drop `.Date` — `x.RefreshTokenExpiresAt >= now` — matching the precision used elsewhere in this file. Should be fixed before this branch merges. |
| P18 | *(escalation note, not a new bug)* `UserProfileController.Get()` now calls `IsTokenValidAsync` — the full-string equality scan on the unindexable `nvarchar(max)` `Token` column from pending **P1** below — on every authenticated profile load, not just a rarely-hit `token/check` endpoint as before. Same root cause and same fix as P1; raises its priority since it now sits on a much hotter path. | `Identity.Api/Controllers/UserProfileController.cs:20-39` (calls into P1's `IsTokenValidAsync`) | No separate fix — implement P1's decided design (lookup by `tid` claim PK + in-memory equality) sooner given the wider blast radius. |
| P19 | `UserService.SearchAsync` orders by `x.Created` (`OrderByDescending`) before paging — no index exists on `User.Created` (`EntityBuilderExtensions.BuildEntities`; ASP.NET Identity's default indexes only cover normalized `UserName`/`Email`). Every page beyond the first forces a full sort of the `Users` table. | `Identity.Api/Services/UserService.cs:31`, `Identity.Api/Data/EntityBuilderExtensions.cs:13-20` | Add `entity.HasIndex(x => x.Created)` (or a composite `(Created, UserName)` index matching the `OrderByDescending(...).ThenBy(...)` shape) and generate a migration. |
| P20 | The search predicate itself (5 OR'd `Contains` over `UserName`/`FirstName`/`LastName`/`Email`/`PhoneNumber`) is a guaranteed full-table scan regardless of P19 — leading-wildcard `Contains` can't be served by a plain B-tree index, and no minimum search-length guard exists, so even a 1-character search triggers an unbounded 5-column scan. | `Identity.Api/Services/UserService.cs:22-30`, `Identity.Contracts/SearchUserQuery.cs` | If this needs to scale beyond current admin-facing usage: PostgreSQL trigram GIN index (`pg_trgm`) or SQL Server full-text index (no in-place EF Core fix exists for `Contains` on a B-tree index). At minimum, enforce a minimum search length (e.g. 2–3 chars) at the query boundary. |
| P21 | No upper bound on `pageSize` anywhere in the pipeline — `ToPagedAsync` (`Persistence/Extensions/QueryableResultExtensions.cs`) only floors it (`<=0 → 10`), never caps it; `PageQuery` (`Shared/PageQuery.cs`) has no `[Range]`/max validation; and no validation pipeline (FluentValidation/DataAnnotations) is wired up anywhere under `Infrastructure` to enforce one even if added. `UserController.SearchAsync` is the **first** controller action to wire `PageQuery` end-to-end, so there's no precedent capping it — a caller can request `pageSize=1000000` and materialize every user in one query. | `Persistence/Extensions/QueryableResultExtensions.cs`, `Shared/PageQuery.cs`, `Identity.Api/Controllers/UserController.cs:17-23` | Clamp `pageSize` to a sane max (e.g. 100–200) in `ToPagedAsync` itself (module-wide fix, protects any future consumer too). Separately worth flagging to whoever owns cross-cutting API conventions: no request-model validation pipeline exists at all yet. |
| P22 | `SearchUserQuery.SearchValue` has no max-length constraint — an unbounded string feeds directly into four `Contains()` predicates. Low severity alone (EF-parameterized, not injectable), but combined with P21's uncapped `pageSize` it's an easy amplification lever. | `Identity.Contracts/SearchUserQuery.cs:5` | Add a length guard (e.g. `[StringLength(256)]`) — note this needs the same validation-pipeline fix as P21 to actually take effect. |
| P23 | `UserController.SearchAsync` is declared `[HttpPost("search")]` but binds *everything* (`SearchUserQuery` and `PageQuery`) via `[FromQuery]` — no request body is used, so it's functionally a `GET` exposed as `POST` (non-cacheable, inconsistent with the controller's GET-for-reads convention elsewhere). Minor/low: this also means `SearchValue` (potentially email/phone) ends up in the URL and thus in web-server/proxy access logs — though that pattern already exists on other endpoints in this controller (e.g. `by_username/{username}`), so it's not a new class of exposure. | `Identity.Api/Controllers/UserController.cs:17-23` | If no request-body use is planned, switch to `[HttpGet("search")]`. If `POST` was chosen deliberately for future body-based filters, leave a short comment saying so. |
| P24 | `AuthenticationService.RefreshTokenAsync` reads `jwtOptions.Value.Issuer`/`SecretKey` directly instead of the cached `_jwt` field used everywhere else in the class (`GetTokenAsync`, and elsewhere in `RefreshTokenAsync` itself). Harmless today (`IOptions<T>.Value` is a stable snapshot) but a needless second access path that would silently diverge if `_jwt` were ever swapped for `IOptionsMonitor`/`IOptionsSnapshot`. | `Identity.Api/Jwt/AuthenticationService.cs:71-72` | Use `_jwt.Issuer`/`_jwt.SecretKey` for consistency with the rest of the class. |
| P25 | `UserSession.TokenExpiresInSeconds` still computes via `DateTimeOffset.Now` — a third, non-injectable clock source in the same feature slice that just migrated `AuthenticationService`/`UserSessionService` to `TimeProvider` (P15/P16). Not incorrect (offset subtraction is offset-invariant) but makes this value non-deterministic in tests that fake `TimeProvider`. | `Identity.Api/Entities/UserSession.cs:30` | Compute `TokenExpiresInSeconds` from the caller (where `TimeProvider` is already available) instead of inside the entity, or accept a `DateTimeOffset now` parameter. |
| P26 | `JwtServiceCollectionExtensions`'s null-guard names the wrong parameter: `ArgumentNullException.ThrowIfNull(jwtSettings, nameof(JwtOptions))` reports the type name, not the null variable, in the thrown exception's `ParamName`. | `Identity.Api/Jwt/JwtServiceCollectionExtensions.cs:16` | `ArgumentNullException.ThrowIfNull(jwtSettings, nameof(jwtSettings))`. |
| P27 | `UserService.SearchAsync`'s `Contains` filters are case-insensitive under SQL Server's default collation but case-sensitive under PostgreSQL's default collation — this module supports both providers (per the existing Sqlite/PostgreSQL migration note above), so search behavior would silently differ by deployment target. | `Identity.Api/Services/UserService.cs:22-30` | Document the expected behavior or normalize explicitly (e.g. `ILike`/`EF.Functions.ILike` on PostgreSQL, or a case-insensitive collation on the columns) if cross-provider consistency matters. |

### P1 — lookup by PK, not by indexing the token string (decided, not yet implemented)

`Token` (on `UserSession`) stores the full JWT (`UserSessionService.cs`), an unbounded string that grows with the number of role/permission claims a user has — realistically over 1500 characters for users with several roles. With no `HasMaxLength` configured, EF Core maps this to `nvarchar(max)` on SQL Server, and **SQL Server cannot index an `nvarchar(max)` column at all**; PostgreSQL's btree index has a similar ~2704-byte per-entry cap that fails at insert time instead. `RefreshToken`, by contrast, is a fixed ~44-char Base64 string and is safe to index directly.

Better design (no schema/index changes needed at all):
- Every issued token already embeds its own `UserSession.Id` as a `tid` claim (`ClaimTypeConstants.TokenId`, set in `JwtTokenIssuer.cs`), and `Id` is already the primary key (indexed for free).
- Parse the `tid` claim out of the presented token, fetch the single row by PK (`context.UserSessions.FindAsync(tokenId)` — no new index required), then compare `entity.Token == accessToken` (and `entity.RefreshToken == refreshToken` for the refresh flow) **in memory**, plus check `Revoked`/expiry.
- Safe even though `tid` can be read from an unverified JWT (e.g. in `IsTokenValidAsync`, now called from `UserProfileController.Get()`): the trust boundary is the final full-string equality check against the securely-stored original — forging a `tid` doesn't help an attacker, since they'd still need the exact original signed token content, which requires the signing key.
- In the refresh flow, `tid` is available for free from the already-signature-validated `userPrincipal` (`JwtHelper.GetPrincipalFromExpiredToken`, `AuthenticationService.cs:63`) — just extract one more claim.
- Minor optional hardening: once comparison moves from SQL `=` to in-memory C# `==`, consider `CryptographicOperations.FixedTimeEquals` to avoid a timing side-channel on the secret token value.

---

## Dependency / package hygiene

- **Likely dead package references** (no source usage found): `Lightsoft.EventBus` (`Shared.csproj`), `Lightsoft.FileGenerator` (`Infrastructure.csproj`). Verify and remove if confirmed unused.
- **Undeclared transitive dependencies** — several projects compile only because a `ProjectReference` happens to bring a package along, not because they declare it themselves:
  - `Infrastructure` uses `Mapster` and `Lightsoft.AspNetCore.Authorization`/`Lightsoft.Result` without declaring either (rides on `Shared`'s references).
  - `Identity.Api` uses `Lightsoft.AspNetCore.Authorization`, `Lightsoft.Result`, `Lightsoft.Extensions` (via `GlobalUsings.cs`) without declaring any of them.
  - **New site, 2026-08-01**: `UserService.SearchAsync`'s new `WhereIf` call (`Identity.Api/Services/UserService.cs:1,22`) uses `Light.Specification`, which is provided by the `Lightsoft.EntityFrameworkCore` package — declared by `Persistence.csproj`, not by `Identity.Api.csproj`, and only compiles today because it rides in transitively through the `Persistence` `ProjectReference`.
  - `StarterKit.WebApi` uses `Lightsoft.Serilog` without declaring it (only `Infrastructure.csproj` does).
  - Fragile: if the upstream project ever drops one of these packages, downstream projects break with no direct signal why. Recommend each project explicitly declare what it directly uses.
- `tests/Framework.Tests` currently references only `Shared`, `Infrastructure`, `Persistence` — **no test coverage exists for `Identity.Api`/`Identity.Contracts`**.

## Housekeeping

- `src/Identity.Api/obj/` and `src/Identity.Contracts/obj/` still contain stale build artifacts referencing pre-refactor assembly names (`Identity.Core.AssemblyInfo.cs`, `StaterKit.Identity.EntityFrameworkCore.*` — note the "StaterKit" typo). Run `dotnet clean` (or delete `bin`/`obj`) to clear these.
- `UserSession` (renamed from `JwtToken`) still extends the plain vendor `Entity` base type — no decision made yet on whether it should carry an audit trail via `AuditableEntity`.
- `Entities/UserToken.cs` (ASP.NET Core Identity's built-in `IdentityUserToken<string>` — external-login-provider token store, wired only as an `IdentityDbContext` generic parameter/table mapping, no business logic touches it) sits in the same `Entities/` folder as `UserSession` (this module's actual JWT/session entity) — no functional coupling, but "token" means two unrelated things in this module. Low-cost mitigation if it comes up again: a one-line comment on `UserToken.cs` noting it's unrelated to JWT sessions, or grouping ASP.NET Identity's built-in entities (`UserClaim`, `UserRole`, `UserLogin`, `RoleClaim`, `UserToken`) separately from module-specific ones (`User`, `Role`, `UserSession`).

---

## Out of scope

- `src/Migrations/**` — excluded per request, except where a change elsewhere (e.g. the `UserSession` rename) forced a mechanical edit there.
