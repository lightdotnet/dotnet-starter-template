/** Mirrors `Notifications.Contracts/Authorization/NotificationPermissions.cs` — keep the string values in sync with the backend. */
export const NOTIFICATIONS_PERMISSIONS = {
  Read: "notification.read",
  Send: "notification.send",
} as const;
