/** Mirrors `Identity.Contracts/Authorization/IdentityPermissions.cs` (Roles.*) — keep the string values in sync with the backend. */
export const ROLES_PERMISSIONS = {
  View: "identity.roles.view",
  Manage: "identity.roles.manage",
} as const;
