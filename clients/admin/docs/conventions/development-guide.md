# Development Guide: admin

## Prerequisites

- **Node.js**: version not pinned anywhere — no `"engines"` field in `package.json`, no `.nvmrc`/`.node-version`. `@types/node` is `^20`, suggesting Node 20.x is the target, but this is inferred, not enforced — `unknown`/unverified.
- **pnpm**: required — `pnpm-lock.yaml` is the only lockfile present. Exact pnpm version is `unknown` (no `packageManager` field in `package.json`). `pnpm-workspace.yaml` only configures build-script approval (`allowBuilds`/`ignoredBuiltDependencies` for `sharp`, `unrs-resolver`) — no effect on the install/run flow.
- **Backend must be reachable**: this app makes real calls to the backend for login and profile data. There is no single `API_BASE_URL` — `IDENTITY_API_BASE_URL` and `NOTIFICATIONS_API_BASE_URL` must each point at a running instance (currently the same co-hosted backend, `http://localhost:5000`) for auth/profile/notification pages to work. The SignalR notification hub also needs the backend reachable **directly from the browser** (not just server-side) — `SIGNALR_HUB_URL` must resolve, and the backend must allow CORS for the admin app's origin.

## Building

Verified from `package.json` scripts:

```bash
pnpm build   # runs `next build` (output: "standalone", per next.config.ts)
```

## Running Locally

```bash
pnpm dev     # runs `next dev`
```

No `--turbopack` flag is set explicitly, and `next.config.ts` has no bundler override — which bundler `next dev` uses by default for this Next.js version is `unknown` from repo config alone.

The dev server listens on Next.js's default port (3000) — no custom port configured.

```bash
pnpm start   # runs `next start` (serves a production build made via `pnpm build`)
```

## Deploying

Three PowerShell scripts at the `clients/` level (repo, not `clients/admin/`) deploy this app's standalone build to a Windows host:

- `clients/deploy-nssm.ps1` — full deploy to an IIS + NSSM-Windows-Service host: `pnpm install --frozen-lockfile` → `pnpm run build` → copy `.next/standalone` (+ `.next/static`, `public/`) into the live service folder, preserving any deployed `.env*` → stop/start the IIS app pool, site, and Windows service. Wipes only the `standalone/` subfolder each run. Requires the IIS `WebAdministration` module and a pre-registered NSSM service.
- `clients/deploy-pm2.ps1` — the PM2-process-manager variant of the same flow.
- `clients/init-nssm.ps1` — one-time NSSM service registration.

Both deploy scripts run `Get-ServerActionsEncryptionKey` first, hoisting `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` out of the preserved deployed `standalone/.env` into the build environment before `pnpm run build`, so Server Action IDs stay stable across deploys (a missing key only warns). Generate a real key once per server (`openssl rand -base64 32`), put it in the deployed `standalone/.env`, and never change it.

## Running Tests

None — no test runner is installed (`package.json` has no `test` script, no Jest/Vitest/Playwright dependency), and no `*.test.*`/`*.spec.*` files exist under `clients/admin` outside `node_modules`.

## Local Setup

- `.gitignore` ignores `.env*` wholesale except `.env.example`, which is committed and documents the app's env vars — all server-only (runtime) except one build-time-only var; none are client-exposed:
  - `IDENTITY_API_BASE_URL` — base URL for the Identity backend module (e.g. `http://localhost:5000/api/v1/`); must include its own full path prefix and a trailing slash — `lib/server/http.ts` does not prepend `api/v1/` itself, so the base URL owns that prefix; never expose via `NEXT_PUBLIC_`.
  - `NOTIFICATIONS_API_BASE_URL` — base URL for the Notifications backend module (e.g. `http://localhost:5000/api/v1/`), same full-prefix/trailing-slash requirement as above; currently points at the same co-hosted backend as `IDENTITY_API_BASE_URL` but is configured independently, since Identity.Api and Notifications.Api are modeled as two separate backend modules; never expose via `NEXT_PUBLIC_`.
  - `TOKEN_ENCRYPTION_KEY` — a 32-byte base64 key (generate with `openssl rand -base64 32`, per `.env.example`'s own comment); `lib/server/config.ts`'s `getTokenEncryptionKey()` throws if it's unset, and it's read on every request — `lib/server/token-cipher.ts` uses it to AES-256-GCM encrypt/decrypt the `admin_session` cookie, exercised by `modules/identity/auth/api/login-action.ts` (encrypt, on login), `src/proxy.ts` (decrypt on every request), and `modules/identity/auth/api/ensure-fresh-session-action.ts` (re-encrypt via `persistSessionCookie()` after a token refresh or profile refetch). The app fails at runtime without a valid key set.
  - `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` — a base64 AES key Next.js uses at **build time** to salt Server Action IDs and encrypt action closure variables. Not read by any app code — Next.js reads it, and the deploy scripts hoist it into the build env (see Deploying above). If unset, Next.js mints a fresh random key per build, so every deploy rotates all Server Action IDs and open tabs on the previous build hit "Failed to find Server Action" until reloaded (the deploy-recovery flow in `lib/shared/deployment-recovery.ts` handles that case gracefully, but a stable key avoids it). Optional for local dev; must be set and constant on deployed servers.
  - `SIGNALR_HUB_URL` — absolute URL to the backend's SignalR hub (e.g. `http://localhost:5000/signalr-hub`); deliberately not `NEXT_PUBLIC_`-prefixed — `lib/server/config.ts`'s `getSignalRHubUrl()` reads it server-side and `get-signalr-token-action.ts` hands it to the browser at connect time (alongside the SignalR access token) via a Server Action, so it can be changed by editing the deployed server's `.env` and restarting, with no rebuild. `use-notifications.ts` connects the browser directly to that URL, bypassing the Next.js server entirely.
- A `.env.local` file exists locally (gitignored) — not read for this doc since it may contain real values; use `.env.example` as the template.
- `pnpm lint` runs ESLint per `eslint.config.mjs`.

## Common Tasks

| Task | How |
|---|---|
| Add a backend migration | Not applicable to this client app — see `../../../../src/docs/conventions/development-guide.md` (out of scope for this doc). |
| Run the API locally | Not applicable to this client app — but note the dev server depends on it being reachable at `IDENTITY_API_BASE_URL`/`NOTIFICATIONS_API_BASE_URL` for login/profile/notifications to function. |
| Run this client app's dev server | `cd clients/admin && pnpm install && pnpm dev` (copy `.env.example` to `.env.local` first and fill in `IDENTITY_API_BASE_URL`/`NOTIFICATIONS_API_BASE_URL`/`TOKEN_ENCRYPTION_KEY`) |
| Deploy this client app | Run `clients/deploy-nssm.ps1` (or `deploy-pm2.ps1`) from a Windows host with the right IIS/NSSM prerequisites — see Deploying above. Ensure `NEXT_SERVER_ACTIONS_ENCRYPTION_KEY` is in the deployed `standalone/.env` first. |
| Add a new backend endpoint call | Add a function to the relevant feature/module's consolidated `<name>.api.ts` (e.g. `modules/identity/users/api/users.api.ts`) — create that file if the feature/module doesn't have one yet — wrapping `requestJson`/`requestVoid` from whichever named instance `lib/server/backend-api.ts` exports for the endpoint's backend module (`identityApi`, `notificationsApi`, `organizationApi`, or `approvalApi`, all pre-wired with bearer-token auth — do **not** import from `lib/server/http.ts` directly unless you're one of the pre-session-cookie exceptions, see "Server-only API plumbing" below) via `guardCall`/`guardResponseCall`/`guardRawCall` (`lib/server/call-guard.ts`); export it from the feature/module's `index.ts` barrel. |
| Add a new feature/module | Create `src/modules/<domain>/<name>/` (or, for a genuinely standalone one, `src/modules/<name>/` the way `approvals` and `notifications` are) with `api/`, optionally `components/`/`types/`, and an `index.ts` barrel; add a route under `src/app/` that re-exports the feature's page component as `default`. `home` is the one legacy exception still living at `src/features/home/` — new features/modules should follow the `src/modules/` convention, not add further top-level `src/features/*` entries. |
| Gate a page on a permission | Call `lib/server/require-permission.tsx`'s `requirePermission(permission)` at the top of the page's async Server Component, then `if (denied) return denied;` before the page's own data fetch — see `users-page.tsx`/`roles-page.tsx`/`notifications-page.tsx` for the reference shape. Don't inline a fresh `resolveSession()`/permission-check/`Empty` block per page; that's the pattern this helper replaced. |
| Add a new list/table page | Reuse `components/shared/data-table` (`DataTable<TData>`) — define `DataTableColumn<TData>[]`, pass `data`/`isLoading`/`error`/pagination props/callbacks. Reference patterns exist: `modules/identity/users/components/{users-page,users-data-table}.tsx` (server-driven search via URL params + `useTransition`) for a single text-field search against a backend search endpoint; `modules/identity/roles/components/{roles-page,roles-data-table}.tsx` (client-side filtering over a full list) when there's no backend search endpoint; `modules/notifications/components/notifications-data-table.tsx` for a custom multi-field filter UI (e.g. multiple selects) via `DataTable`'s `customSearch`/`onCustomSearch` props; and `modules/approvals/components/{my-approvals-table,all-approvals-table}.tsx` for the simplest shape — no search/pagination props at all, just a pre-fetched array plus a manual `onRefresh`. |
| Show a toast notification | Call `notifySuccess`/`notifyError` from `@/components/toast` — don't import `sonner` directly; `AppToaster` is already mounted once in `app/layout.tsx`. |
| Add an imperative (non-form) action with toast + pending feedback | Use `hooks/use-guarded-action.ts`'s `useGuardedAction()` (`[pending, run]`) instead of hand-rolling `useTransition` + manual `notifyError`/`notifySuccess` branching — see `delete-user-dialog.tsx`/`delete-role-dialog.tsx`. |
| Add a form dialog bound to a mutation Server Action | Bind `useActionState` to the action as usual, then call `hooks/use-action-success-toast.ts`'s `useActionSuccessToast(state, successMessage, onSuccess?)` instead of a hand-written `useEffect` — see any of the create/edit dialogs (`create-user-dialog.tsx`, `create-role-dialog.tsx`, `edit-role-dialog.tsx`, `edit-user-dialog.tsx`, `send-notification-dialog.tsx`). |
| Add a new shadcn UI primitive | `npx shadcn@latest add <component>` from `clients/admin/` (`components.json`: style `radix-nova`, base color `neutral`, icon library `lucide`, `css: "src/app/globals.css"`, `utils` alias `@/lib/shared/utils`) — `components/ui/button.tsx` has manual edits (`loading` prop, `cursor-pointer`) a regeneration could overwrite; diff before committing after any CLI regen. `popover.tsx`/`command.tsx` were instead hand-written to match this repo's conventions (`data-slot`, `cn`, the `radix-ui` unified-package import style) rather than run through the CLI — worth following that precedent for a primitive the CLI doesn't cleanly support out of the box. |
| Add a new nav item | If it belongs to an existing nav-bearing feature/module, add/update that feature's `constants/nav-item.ts` and re-export it from the feature's `index.ts` barrel; then reference it from `src/constants/nav-items.ts` (`NAV_ITEMS`) **by direct file path**, not via the barrel (see [architecture.md](../architecture/architecture.md#key-design-patterns) for why — the barrel also carries server-only code that must not reach the client-side `Sidebar`). For a brand-new nav-bearing feature/module, create `<its-folder>/constants/nav-item.ts` following the same pattern. Group/no-owning-feature nodes (currently "Administration", "Organization", "Settings") are declared directly in `nav-items.ts`; a standalone leaf not nested in any group (currently only `approvals`) is declared as a bare top-level array element instead. Note: adding a nav entry does not create its route — a matching `src/app/**/page.tsx` is still needed. The entry also automatically appears in the topbar ⌘K search (via `flattenNavLeaves`), gated by the same permission. |
| Lint the app | `pnpm lint` |
| Regenerate this client's typed API client (if applicable) | Not applicable — this app hand-writes its backend calls under each feature/module's `api/` folder (one consolidated file per feature/module, one function per backend endpoint within that file), it doesn't generate a client from an OpenAPI spec. |

## Where to Look for X

- **App shell / global layout**: `src/app/layout.tsx` (fonts, `ThemeProvider`, `AccentColorProvider`, `TooltipProvider`).
- **Dashboard-group layout (session resolution + top bar/sidebar)**: `src/app/(dashboard)/layout.tsx` → `components/layout/app-shell.tsx`.
- **Routes**: `src/app/(dashboard)/page.tsx` (`/`), `src/app/(dashboard)/user-profile/page.tsx` (`/user-profile`), `src/app/login/page.tsx` (`/login`) — all one-line re-exports; real UI lives in the corresponding feature/module folder. The one non-page route is `src/app/api/health/route.ts` (`GET /api/health` → `204`).
- **Login flow**: `modules/identity/auth/` — `api/token.api.ts` (backend call, exports `getToken`/`refreshToken`), `api/login-action.ts` (server action: login, decode JWT permissions/roles, encrypt + set session cookie, redirect to `?redirect=<path>` or `/`), `components/{login-page,login-form}.tsx`.
- **Logout flow**: `modules/identity/auth/api/logout-action.ts` (deletes the session cookie, redirects to `/login?redirect=<path>` or `/login`; note it is **not** exported from the `identity/auth` barrel — `components/layout/user-menu.tsx` imports it directly), `components/layout/user-menu.tsx` ("Log out" menu item, passes the current path so login can redirect back).
- **Session cookie handling (encryption + refresh)**: `lib/server/session-cookie.ts` (cookie name + `SESSION_TTL_MS`/`REFRESH_LEAD_MS`/`MAX_REFRESH_FAILURES`/`MAX_CHUNK_BYTES`/`MAX_CHUNKS`/`ALL_SESSION_COOKIE_NAMES`/`buildSessionCookieOptions`), `lib/server/token-cipher.ts` (AES-256-GCM `encrypt`/`decrypt`, keyed by `TOKEN_ENCRYPTION_KEY`), `lib/server/stored-session.ts` (`toStoredSession()`/`hydrateSession()` — reduces a full `SessionData` down to the minimal persisted `StoredSession` shape and back, since `claims`/`permissions`/`roles` are fully re-derivable from `accessToken`), `lib/server/cookie-codec.ts` (`encodeSessionCookies()`/`decodeSessionCookies()` — encrypt + validate, splitting across numbered chunk cookies past `MAX_CHUNK_BYTES`), `lib/server/session.ts` (`getSession()` — reads and hydrates the cookie(s), no fetch), `lib/server/jwt.ts` (decode permissions/roles/claims from the JWT), `lib/server/build-session-claims.ts`, `lib/server/refresh-session.ts` (`refreshSession()` unconditional rotate + `refreshSessionIfNearExpiry()` near-expiry-gated, both returning a `RefreshOutcome`), `lib/server/refetch-profile.ts` (profile refetch + claims rebuild), `lib/server/persist-session-cookie.ts` (`persistSessionCookie()` — the Server-Action-context cookie write, via `cookie-codec.ts`), `modules/identity/user-profile/api/resolve-session.ts` (thin passthrough to `getSession()`). **`src/proxy.ts` is now only a thin auth gate** — decrypt/validate the cookie, enforce the hard 7-day `sessionExpiresAt` cap, keep `/login` unreachable once authenticated; it no longer refreshes tokens or refetches the profile. That orchestration moved to `components/layout/session-gate.tsx` (`SessionGate`, wrapping `AppShell`) driving `modules/identity/auth/api/ensure-fresh-session-action.ts` — see [overview.md](../architecture/overview.md#auth-flow) for the full flow.
- **Deploy-induced stale-tab recovery**: `lib/shared/deployment-recovery.ts` (`isRecoverableDeploymentError`/`runDeploymentRecovery`/`peekDeploymentRecoveryExhausted`), `components/layout/deployment-recovery-notice.tsx` (`DeploymentRecoveryNotice`), wired into both `src/app/error.tsx` and `src/app/(dashboard)/error.tsx`; polls `src/app/api/health/route.ts`.
- **Profile page**: `modules/identity/user-profile/components/user-profile-page.tsx` (account details, QR code via `qrcode`, roles/claims).
- **Session management API (no UI yet)**: `modules/identity/user-profile/api/user-profile.api.ts`'s `listSessions`/`revokeSession`.
- **User management UI (`/identity/users`)**: `modules/identity/users/components/users-page.tsx` (Server Component: session/permission gate via `lib/server/require-permission.tsx`'s `requirePermission()`, fetches via `users.api.ts`'s `searchUsers`), `modules/identity/users/components/users-data-table.tsx` (client wrapper around the shared `DataTable`, owns the create/edit/delete dialogs' open state), `create-user-dialog.tsx` (create form, plus a domain-user/AD lookup via `get-domain-user-action.ts`), `edit-user-dialog.tsx` (edit + password reset, fetches full detail on open via `get-user-detail-action.ts` — and also self-fetches the role catalog via `get-all-roles-action.ts` on open, since `users-page.tsx` doesn't preload and pass it down), `delete-user-dialog.tsx` (confirm + delete). All of `users-page.tsx`'s and the dialogs' underlying backend calls go through one consolidated `modules/identity/users/api/users.api.ts`.
- **Role management UI (`/identity/roles`)**: `modules/identity/roles/components/roles-page.tsx` (Server Component: permission gate via `requirePermission()`, fetches all roles via `roles.api.ts`'s `getAllRoles`), `roles-data-table.tsx` (client wrapper, client-side search filter — no backend search endpoint), `create-role-dialog.tsx` (name/description only — the backend accepts no claims on create), `edit-role-dialog.tsx` (name/description + a permissions checklist, fetches full role detail on open via `get-role-detail-action.ts` — and also self-fetches the permission catalog via `get-permissions-action.ts` on open, rather than it being sourced from the page), `delete-role-dialog.tsx`. All of these dialogs' underlying backend calls go through one consolidated `modules/identity/roles/api/roles.api.ts`.
- **Real-time notifications (topbar bell)**: `modules/notifications/hooks/use-notifications.ts` (SignalR connection, refresh-on-push), `modules/notifications/components/notification-bell.tsx` (bell icon, unread badge/ring animation, All/Unread/Archived tab filter).
- **Notification management UI (`/notifications`)**: `modules/notifications/components/notifications-page.tsx` (Server Component: permission gate via `requirePermission()`, converged on the same shared `AccessDenied` behavior as Users/Roles, fetches via `notifications.api.ts`'s `getNotifications`), `notifications-data-table.tsx` (status + recipient filter dropdowns wired to URL params), `send-notification-dialog.tsx` (the "Send" action, gated separately on `NOTIFICATIONS_PERMISSIONS.Send`), `user-select.tsx` (recipient/sender picker shared by both — an on-demand search component backed by `search-users-action.ts`, rather than a shared picker over page-preloaded data). Admin-facing calls (`getNotifications`/`sendNotification`) live in `modules/notifications/api/notifications.api.ts`; self-service calls used by the bell/inbox (`getMyNotifications`/`getUnreadCount`/`markNotificationRead`) live in a separate `modules/notifications/api/user-notifications.api.ts`.
- **Organization administration (`/organization/{companies,departments,employees}`)**: `modules/organization/companies/` (plain CRUD, closest analogue to `roles/`), `modules/organization/departments/` (`org-unit-tree.tsx`'s recursive department/team tree, including a "View managers" dialog fetched on demand via `get-org-unit-managers-action.ts`), `modules/organization/employees/` (`edit-employee-dialog.tsx`'s tabbed Details/Departments & Teams/Login dialog — the org-unit-membership tab now also collects a `Current`/`Acting` status and a manager flag per assignment).
- **Approvals UI (`/approvals`)**: `modules/approvals/components/approvals-page.tsx` (Server Component: permission gate via `requirePermission()`, fetches both tables' data server-side), `my-approvals-table.tsx`/`all-approvals-table.tsx` (plain client components rendering a pre-fetched array through the shared `DataTable`, no search/pagination props), `decide-approval-dialog.tsx` (approve/reject + optional comment), `create-approval-request-dialog.tsx` + `approver-select.tsx` (the "Create test request" admin harness, building an ordered approver chain). All backend calls go through `modules/approvals/api/approvals.api.ts`.
- **Select / combobox input**: `components/ui/combobox.tsx` (`Combobox<TValue>`, a Popover+Command single-select) plus its `components/ui/{popover,command}.tsx` primitives. Consumed only by `modules/identity/users/components/edit-user-dialog.tsx` (status/authProvider) — the three on-demand user-search components (`modules/notifications/components/user-select.tsx`, `modules/organization/employees/components/user-select.tsx`, `modules/approvals/components/approver-select.tsx`) each use a bespoke `components/ui/{button,popover,command}.tsx` composition instead. Nested inside a `Dialog`, these all portal into the Dialog's own DOM node via `components/foundation/portal-container.ts` rather than `document.body` — see `components/ui/dialog.tsx` if extending this pattern to another Radix overlay.
- **Command palette (⌘K page search)**: `components/command/command-palette.tsx` (`CommandPalette`, built on `components/foundation/*` + `@floating-ui/react`), wired into the app by `components/shared/search-box.tsx` in the topbar — which owns the open state, the global keydown listener, and builds the entries from `buildVisibleMenu(NAV_ITEMS, can)` + `flattenNavLeaves`. The sibling `components/command/command-palette-provider.tsx` (`CommandPaletteProvider`) is an unused alternative.
- **Reusable list/table building block**: `components/shared/data-table/` (`DataTable`, toolbar, `data-table-buttons.tsx` for the Export/Refresh/Columns cluster — built on `components/ui/button-group.tsx` — pagination, `types.ts` — including optional per-column client-side sort via `sortable`/`sortValue`, and an optional `customSearch`/`onCustomSearch` slot for a caller-supplied, apply-on-click multi-field filter UI, used by `NotificationsDataTable`) — generic, no feature/module knowledge; used by `modules/identity/{users,roles}`, `modules/notifications`, `modules/organization/{companies,departments,employees}`, and `modules/approvals`.
- **Reusable read-only object/table viewer (not yet wired into any page)**: `components/shared/object-viewer/` (`ObjectViewer`, plus `object-viewer-row.tsx`, `column-resize-handle.tsx`, `object-viewer-layout-context.tsx`, `utils.ts`) — purely additive, built on `components/ui/table`/`components/ui/input`, no feature/module dependency.
- **Toast notifications**: `components/toast/` (`AppToaster` mounted in `app/layout.tsx`, `notifySuccess`/`notifyError`, saturated color theme in `toast-theme.ts`).
- **Permission checks**: `lib/shared/authorization.ts` (`hasPermission`/`hasAnyPermission`/`hasAllPermissions`, `isSuperAdminUser`, `SUPER_ADMIN_USERNAMES` bypass list — takes `permissions: string[]`/`userName` directly, safe for both server and client) is the actual logic; `lib/server/authorization.ts` is a thin server-only wrapper that derives `userName` from `session.profile?.userName` internally and is called as `hasPermission(session, permission)`/`hasAnyPermission(session, permissions)`/`hasAllPermissions(session, permissions)` — used by every gated page. `lib/server/require-permission.tsx`'s `requirePermission(permission)` is the higher-level, sanctioned entry point for gating an entire page — see "Gate a page on a permission" above and `components/shared/access-denied.tsx` for the panel it renders. `components/layout/sidebar.tsx` and `components/shared/search-box.tsx` both call `lib/shared/authorization.ts` directly (client-side, no `lib/server/*` involved). Permission-string constants live per-feature/module: `modules/identity/users/constants/permissions.ts` (`USERS_PERMISSIONS`), `modules/identity/roles/constants/permissions.ts` (`ROLES_PERMISSIONS`), `modules/notifications/constants/permissions.ts` (`NOTIFICATIONS_PERMISSIONS` — `notification.read`/`notification.send`), `modules/organization/{companies,departments,employees}/constants/permissions.ts` (`COMPANIES_PERMISSIONS`/`ORG_UNITS_PERMISSIONS`/`EMPLOYEE_LEVELS_PERMISSIONS`/`EMPLOYEES_PERMISSIONS`), and `modules/approvals/constants/permissions.ts` (`APPROVALS_PERMISSIONS`) — each mirrors its module's own backend permission-string constants.
- **Home page (session-backed profile summary + notification inbox)**: `features/home/` — `components/home-page.tsx` (async Server Component: resolves the session, redirects to `/login` if absent, renders `ProfileSummaryCard` plus an `Inbox` card wrapping `NotificationInbox`, whose initial page is fetched server-side via `getMyNotificationsAction()`), `components/profile-summary-card.tsx` (account summary: avatar initials, display name, `@username`, status/role badges). Routed at `/` (`src/app/(dashboard)/page.tsx`). No mock/sample data — both the profile summary and the inbox come from the real session and the live `user_notification` endpoint.
- **Top bar**: `components/layout/topbar.tsx` (brand, breadcrumbs, the ⌘K search trigger — `SearchBox`, passed `permissions`/`userName` — accent picker, theme toggle, notifications, user menu).
- **Sidebar (nav + show/hide + mobile drawer)**: `components/layout/sidebar.tsx` (also computes the permission-filtered menu via `lib/shared/menu.ts`'s `buildVisibleMenu()`), `components/layout/sidebar-nav-item.tsx`, state in `hooks/use-sidebar.tsx`. Only the sidebar's `hidden` state persists to `localStorage` — the nav-group expand/collapse overrides (`expandedOverrides`) always reset to the auto-expand-active-route default on reload.
- **Nav structure/labels**: each nav-bearing feature/module owns its own entry — `features/home/constants/nav-item.ts` plus each module's own `constants/nav-item.ts` (`identity/{users,roles}`, `notifications`, `organization/{companies,departments,employees}`, `approvals`); `src/constants/nav-items.ts` (typed via `types/nav.ts`) assembles those into `NAV_ITEMS`, only declaring the "Administration"/"Organization" group nodes and the "Settings" leaf itself, plus placing `APPROVALS_NAV_ITEM` as a bare top-level entry. `lib/shared/menu.ts` exposes `buildVisibleMenu` (permission filter, used by both `Sidebar` and `SearchBox`) and `flattenNavLeaves` (leaf-with-section list, used by `SearchBox`).
- **SignalR direct connection (real-time push)**: `use-notifications.ts` opens a WebSocket directly from the browser to a hub URL (an absolute URL); `next.config.ts` has no `rewrites()` proxy, so this requires the backend to allow CORS for the admin app's origin. `modules/notifications/api/get-signalr-token-action.ts` hands the browser both the short-lived access token needed for the handshake and the hub URL itself — read server-side from the server-only `SIGNALR_HUB_URL` env var (`lib/server/config.ts`'s `getSignalRHubUrl()`), not `NEXT_PUBLIC_`-inlined, so it stays a runtime setting rather than a build-time constant — proactively refreshing a near-expiry session first via `refreshSessionIfNearExpiry()`. A failed handshake retries every 30s (`connectSignalR`), logging a sanitized error instead of the SignalR client's raw HTML-embedding one.
- **Theming (light/dark, accent color)**: `components/theme/` (`theme-provider.tsx`, `accent-color-provider.tsx`, `theme-toggle.tsx`, `accent-color-picker.tsx`, `use-has-mounted.ts`); design tokens in `src/app/globals.css`'s `@theme inline` block and `:root`/`.dark[data-accent=...]` variable declarations.
- **shadcn-generated UI primitives**: `components/ui/*` — configured via `components.json`.
- **Server-only API plumbing**: `lib/server/http.ts` (fetch wrapper, `handlers`/`HttpRequestHandler` pipeline run before `fetch()` — not session-aware itself; does not hardcode an `api/v1/` prefix — the configured base URL owns its full path prefix), `lib/server/backend-api.ts` (exports a `createBackendApiClient(client)` factory plus four ready instances, `identityApi`/`notificationsApi`/`organizationApi`/`approvalApi` — the entry points most modules' `api/*.ts` files use; each wraps `http.ts` with bearer-token auth + its own fixed `client` pre-wired), `lib/server/api-clients.ts` (the `ApiClients` named-backend registry, four entries, `Identity`/`Notifications`/`Organization`/`Approval`), `lib/server/http-handlers/bearer-token-handler.ts` (`bearerTokenHandler`, reads the ambient session; `explicitBearerTokenHandler(accessToken)`, for the pre-session-cookie exceptions — `modules/identity/auth/api/token.api.ts` (`getToken`/`refreshToken`) and `modules/identity/user-profile/api/user-profile.api.ts` (`getCurrentUser`), both passing `client: ApiClients.Identity` explicitly), `lib/server/call-guard.ts` (error-envelope normalization), `lib/server/config.ts` (env access, per-client via `getApiBaseUrl(client)` — `client` is a required parameter, no default), `lib/server/require-permission.tsx` (`requirePermission(permission)` — the shared page-level permission gate, see "Permission checks" above).
- **Cross-cutting helpers**: `lib/shared/utils.ts` (`cn`), `lib/shared/dedupe-claims.ts`, `lib/shared/user-display.ts`, `lib/shared/user-status.ts`, `lib/shared/menu.ts` (`buildVisibleMenu`/`flattenNavLeaves`), `lib/shared/deployment-recovery.ts` (`"use client"` — the deploy-recovery loop for the error boundaries).
- **Backend/client shared shapes**: `types/api.ts` (`Result`/`ApiResponse`/`Paged`/`PagedResult`, mirroring the backend envelope), `types/{session,claim,nav}.ts` (`TokenDto`/`UserDto`/etc. live per-feature/module — `modules/identity/auth/types/token.ts`, `modules/identity/users/types/user.ts` — re-exported via their owning feature/module's barrel, not in a global `types/` file).

## Notes

<!-- manual: content below this line is human-authored and must be preserved verbatim during sync -->

---
_Last synced: 2026-09-05_
